[
  {
    "slug": "virtual-dom",
    "label": "Virtual DOM",
    "topic": "react",
    "tooltip": "An in-memory representation of the real DOM. React diffs old and new versions to compute the minimal set of DOM updates.",
    "definition": "The Virtual DOM is React's in-memory representation of the UI tree. When state changes, React builds a new virtual tree, compares it against the previous one, and applies only the diff to the real DOM. The point is that you can write code as if the whole UI re-renders on every change — React makes it efficient under the hood. This is what allows the declarative model to scale.",
    "codeExample": null,
    "relatedSlugs": [
      "reconciliation",
      "hydration"
    ]
  },
  {
    "slug": "reconciliation",
    "label": "Reconciliation",
    "topic": "react",
    "tooltip": "The algorithm React uses to figure out the minimum DOM operations needed to update the UI when state or props change.",
    "definition": "Reconciliation is the algorithm React uses to diff the previous element tree against the new one. It uses two heuristics: different element types produce different trees (so type changes cause unmount/remount), and stable `key` props identify children across renders. Understanding reconciliation is what makes keys, stable component identities, and subtree-remount bugs click into place.",
    "codeExample": "// Same type → React updates in place\n<button className={on ? 'on' : 'off'}>Toggle</button>\n\n// Type change → React tears down and rebuilds\n{on ? <ButtonOn /> : <ButtonOff />}",
    "relatedSlugs": [
      "virtual-dom"
    ]
  },
  {
    "slug": "hook",
    "label": "Hook",
    "topic": "react",
    "tooltip": "A function (useState, useEffect, etc.) that lets function components opt into React features like state, lifecycle, and context.",
    "definition": "Hooks are functions starting with `use` that let function components access state, side effects, refs, context, and other React features that used to require class components. Rules: call hooks at the top level (no conditionals), only call them from React functions. The hook system is built on calling order — that's why the rules exist.",
    "codeExample": "function Counter() {\n  const [count, setCount] = useState(0);\n  useEffect(() => {\n    document.title = `Count: ${count}`;\n  }, [count]);\n  return <button onClick={() => setCount(c => c + 1)}>+1</button>;\n}",
    "relatedSlugs": [
      "server-component",
      "strict-mode"
    ]
  },
  {
    "slug": "strict-mode",
    "label": "StrictMode",
    "topic": "react",
    "tooltip": "A dev-only wrapper that surfaces problems by double-invoking effects, reducers, and state initialisers.",
    "definition": "`<React.StrictMode>` is a development-only wrapper that intentionally double-invokes function component bodies, class render methods, reducers, and state initialisers. It also runs effects through a setup→cleanup→setup cycle to simulate a remount. The point is to catch effects that aren't idempotent — bugs that would otherwise only show up under concurrent rendering. It's a no-op in production.",
    "codeExample": "<React.StrictMode>\n  <App />\n</React.StrictMode>",
    "relatedSlugs": [
      "hook",
      "reconciliation"
    ]
  },
  {
    "slug": "hydration",
    "label": "Hydration",
    "topic": "react",
    "tooltip": "The process of attaching React's event handlers and state to server-rendered HTML so the page becomes interactive.",
    "definition": "When the server sends pre-rendered HTML, the browser shows it immediately — but it isn't interactive yet. Hydration is the process where React downloads the JS bundle, walks the DOM, and attaches event handlers, restores state, and connects the rendered HTML to its component tree. A hydration mismatch happens when the server-rendered output doesn't match what the client would render — usually caused by reading `window`, `Date.now()`, or other non-deterministic values during render.",
    "codeExample": null,
    "relatedSlugs": [
      "virtual-dom",
      "server-component"
    ]
  },
  {
    "slug": "generic",
    "label": "Generic",
    "topic": "typescript",
    "tooltip": "A type parameter that lets a function or type work with multiple input types while preserving the relationship between them.",
    "definition": "Generics let you write a function or type that works with multiple input types without losing type information. The classic use is a function whose return type depends on its input: `first<T>(arr: T[]): T | undefined` preserves the element type, so `first([1,2,3])` returns `number | undefined`, not `unknown | undefined`. Generics also work on classes, interfaces, and type aliases.",
    "codeExample": "function first<T>(arr: T[]): T | undefined {\n  return arr[0];\n}\n\nconst n = first([1, 2, 3]); // n: number | undefined\nconst s = first(['a', 'b']); // s: string | undefined",
    "relatedSlugs": [
      "type-inference",
      "discriminated-union"
    ]
  },
  {
    "slug": "type-inference",
    "label": "Type Inference",
    "topic": "typescript",
    "tooltip": "The compiler's ability to figure out a type from context, so you don't have to annotate everything by hand.",
    "definition": "TypeScript infers types from initialisers, return values, and function calls so you don't have to annotate every variable. `const x = 42` is inferred as `number`. `users.map(u => u.name)` infers `string[]` if `users` is typed. Over-annotating is the rookie move — let the compiler do the work and only annotate at API boundaries (function parameters, exported function returns).",
    "codeExample": "// No annotations needed — inferred\nconst users = [{ id: '1', name: 'Aaron' }];\nconst names = users.map(u => u.name);\n//    ^? string[]",
    "relatedSlugs": [
      "generic",
      "structural-typing"
    ]
  },
  {
    "slug": "discriminated-union",
    "label": "Discriminated Union",
    "topic": "typescript",
    "tooltip": "A union type where each variant has a literal property the compiler can use to narrow which variant you're holding.",
    "definition": "A discriminated union (or tagged union) is a union type where every member has a shared property holding a unique literal value — the discriminant. The compiler narrows the union based on that discriminant inside conditional checks. This pattern replaces enums + interfaces with something safer and more ergonomic.",
    "codeExample": "type Shape =\n  | { kind: 'circle'; radius: number }\n  | { kind: 'square'; side: number };\n\nfunction area(shape: Shape) {\n  if (shape.kind === 'circle') return Math.PI * shape.radius ** 2;\n  return shape.side ** 2; // shape is narrowed to square here\n}",
    "relatedSlugs": [
      "generic",
      "structural-typing"
    ]
  },
  {
    "slug": "structural-typing",
    "label": "Structural Typing",
    "topic": "typescript",
    "tooltip": "TypeScript matches types by shape, not by name — duck typing with compiler help.",
    "definition": "TypeScript uses structural typing: two types are compatible if they have the same shape, regardless of whether either was declared to implement the other. A plain object literal can satisfy an interface without ever being declared as that interface. This is the opposite of nominal typing in languages like Java or C#.",
    "codeExample": "interface Point { x: number; y: number; }\n\nfunction distance(a: Point, b: Point) { /* ... */ }\n\nconst p = { x: 1, y: 2, label: 'A' };\ndistance(p, p); // ✅ p has the right shape",
    "relatedSlugs": [
      "type-inference",
      "discriminated-union"
    ]
  },
  {
    "slug": "const-assertion",
    "label": "const Assertion",
    "topic": "typescript",
    "tooltip": "`as const` tells TS to infer the narrowest possible type — string literals stay literal, arrays become readonly tuples.",
    "definition": "`as const` narrows inference to the most specific type possible. String literals stay as their literal type, arrays become readonly tuples, object properties become readonly literal types. The classic use is deriving a union type from a runtime constant — define the values once, derive the type from `typeof`.",
    "codeExample": "const STATUS = {\n  pending: 'pending',\n  active: 'active',\n  archived: 'archived',\n} as const;\n\ntype Status = (typeof STATUS)[keyof typeof STATUS];\n// → 'pending' | 'active' | 'archived'",
    "relatedSlugs": [
      "type-inference",
      "discriminated-union"
    ]
  },
  {
    "slug": "app-router",
    "label": "App Router",
    "topic": "nextjs",
    "tooltip": "Next.js 13+ routing system built on React Server Components, living in the app/ directory.",
    "definition": "The App Router is the routing system introduced in Next.js 13 and stabilised in 14. It lives in `app/` and is built on React Server Components by default. Key features: nested layouts that persist across navigation, streaming with Suspense, parallel and intercepting routes, route handlers for APIs, and per-segment loading and error states.",
    "codeExample": "app/\n├── layout.tsx     // wraps everything\n├── page.tsx       // /\n└── blog/\n    ├── page.tsx           // /blog\n    └── [slug]/\n        └── page.tsx       // /blog/:slug",
    "relatedSlugs": [
      "server-component",
      "route-handler",
      "isr"
    ]
  },
  {
    "slug": "server-component",
    "label": "Server Component",
    "topic": "nextjs",
    "tooltip": "A React component that renders on the server and never ships its JS to the browser. Default in App Router.",
    "definition": "A React Server Component renders on the server (at build or request time) and never ships its JavaScript to the browser. It can fetch data directly, read from databases, access environment variables — anything server-side. The browser receives just the rendered HTML / RSC payload. In Next.js App Router, components are Server Components by default; you opt into client behaviour with `'use client'`.",
    "codeExample": "// Server Component — no 'use client'\nexport default async function ProductList() {\n  const products = await db.product.findMany();\n  return <ul>{products.map(p => <li key={p.id}>{p.name}</li>)}</ul>;\n}",
    "relatedSlugs": [
      "app-router",
      "hydration"
    ]
  },
  {
    "slug": "isr",
    "label": "ISR",
    "topic": "nextjs",
    "tooltip": "Incremental Static Regeneration — serve static pages but refresh them in the background after a time window or on-demand.",
    "definition": "ISR (Incremental Static Regeneration) lets you serve statically generated pages but refresh them after a TTL or via on-demand invalidation. You get CDN-speed responses with eventual freshness. Configure with `export const revalidate = N` for time-based refresh, or `revalidatePath` / `revalidateTag` for on-demand invalidation when underlying data changes.",
    "codeExample": "// Time-based ISR\nexport const revalidate = 60; // seconds\n\n// On-demand from a webhook\nimport { revalidateTag } from 'next/cache';\nrevalidateTag('products');",
    "relatedSlugs": [
      "app-router",
      "server-component"
    ]
  },
  {
    "slug": "route-handler",
    "label": "Route Handler",
    "topic": "nextjs",
    "tooltip": "App Router replacement for API routes. A route.ts file exporting HTTP method functions using standard Web Request/Response.",
    "definition": "Route Handlers are the App Router replacement for `pages/api/` routes. A `route.ts` file inside `app/` exports HTTP method functions (`GET`, `POST`, `PATCH`, etc.) that use the standard Web `Request` and `Response` objects rather than Next-specific `req`/`res`. This makes them portable and easier to test.",
    "codeExample": "// app/api/users/route.ts\nexport async function GET() {\n  const users = await db.user.findMany();\n  return Response.json(users);\n}\n\nexport async function POST(request: Request) {\n  const body = await request.json();\n  const user = await db.user.create({ data: body });\n  return Response.json(user, { status: 201 });\n}",
    "relatedSlugs": [
      "app-router",
      "server-component"
    ]
  },
  {
    "slug": "middleware",
    "label": "Middleware",
    "topic": "nextjs",
    "tooltip": "Code that runs at the Edge before a request reaches a route — used for auth gates, redirects, A/B tests, locale negotiation.",
    "definition": "Middleware runs at the Edge before a request hits a route. The function receives a `NextRequest` and returns a `NextResponse` — it can redirect, rewrite, set cookies, or modify request headers. Configure which routes it runs on via a `matcher` config. In Next 16+, the file is named `proxy.ts` (same API). Keep it small — runs on every matched request.",
    "codeExample": "// middleware.ts (or proxy.ts in Next 16+)\nimport { NextResponse } from 'next/server';\nimport type { NextRequest } from 'next/server';\n\nexport function middleware(request: NextRequest) {\n  const token = request.cookies.get('session');\n  if (!token) return NextResponse.redirect(new URL('/login', request.url));\n  return NextResponse.next();\n}\n\nexport const config = { matcher: ['/dashboard/:path*'] };",
    "relatedSlugs": [
      "app-router",
      "route-handler"
    ]
  },
  {
    "slug": "redux-store",
    "label": "Redux Store",
    "topic": "redux",
    "tooltip": "The single object that holds the entire application state tree in a Redux app.",
    "definition": "The Redux store is the single source of truth for application state. Created with `configureStore()` (Redux Toolkit) or the legacy `createStore()`, it exposes three core methods: `getState()` to read the current state, `dispatch(action)` to trigger a state change, and `subscribe(listener)` to react to changes. There is always exactly one store per Redux application — multiple data domains are handled by combining slice reducers, not by creating multiple stores.",
    "codeExample": "import { configureStore } from '@reduxjs/toolkit';\nimport cartReducer from './cartSlice';\nimport userReducer from './userSlice';\n\nexport const store = configureStore({\n  reducer: { cart: cartReducer, user: userReducer },\n});\n\nexport type RootState  = ReturnType<typeof store.getState>;\nexport type AppDispatch = typeof store.dispatch;",
    "relatedSlugs": [
      "redux-reducer",
      "redux-action",
      "redux-dispatch"
    ]
  },
  {
    "slug": "redux-reducer",
    "label": "Reducer",
    "topic": "redux",
    "tooltip": "A pure function (state, action) => newState that describes how state changes in response to an action.",
    "definition": "A reducer is a pure function that takes the current state and an action object and returns the next state — never mutating the original. The name comes from `Array.prototype.reduce`: just as reduce folds an array into a single value, a Redux reducer folds a stream of dispatched actions into the current state. Rules: no side effects, no direct mutations, always return the current state unchanged for unknown action types. Redux Toolkit's `createSlice` generates reducers with Immer baked in, so you can write mutating-looking code that produces new immutable state.",
    "codeExample": "import { createSlice, PayloadAction } from '@reduxjs/toolkit';\n\nconst counterSlice = createSlice({\n  name: 'counter',\n  initialState: { value: 0 },\n  reducers: {\n    increment: (state) => { state.value += 1; },\n    setBy:     (state, action: PayloadAction<number>) => {\n      state.value = action.payload;\n    },\n  },\n});\n\nexport const { increment, setBy } = counterSlice.actions;\nexport default counterSlice.reducer;",
    "relatedSlugs": [
      "redux-store",
      "redux-action",
      "immer",
      "combine-reducers"
    ]
  },
  {
    "slug": "redux-action",
    "label": "Action",
    "topic": "redux",
    "tooltip": "A plain JS object with a `type` field that describes what happened in the app.",
    "definition": "An action is the only way to describe a state change in Redux — a plain object with at minimum a `type` string. By convention (and Redux Toolkit's Flux Standard Action shape) it also carries a `payload` field for data. Actions are dispatched to the store, which passes them through middleware and then to the root reducer. With `createSlice`, action creators are auto-generated and fully typed — you rarely write action objects by hand.",
    "codeExample": "// Manual action object\nstore.dispatch({ type: 'cart/addItem', payload: { id: '42', qty: 1 } });\n\n// Auto-generated action creator (Redux Toolkit)\nimport { addItem } from './cartSlice';\nstore.dispatch(addItem({ id: '42', qty: 1 }));\n// dispatches: { type: 'cart/addItem', payload: { id: '42', qty: 1 } }",
    "relatedSlugs": [
      "redux-dispatch",
      "redux-reducer",
      "redux-store"
    ]
  },
  {
    "slug": "redux-dispatch",
    "label": "Dispatch",
    "topic": "redux",
    "tooltip": "The store method that sends an action through middleware and into the root reducer to update state.",
    "definition": "`store.dispatch(action)` is the only way to trigger a state change. It sends the action through the middleware pipeline (thunk, logger, etc.) and then calls the root reducer with the current state and that action. In React components, use the typed `useAppDispatch` hook rather than importing the store directly — this keeps components decoupled from the store singleton and ensures correct TypeScript inference.",
    "codeExample": "// In a component\nimport { useAppDispatch } from '@/app/hooks';\nimport { increment } from './counterSlice';\n\nfunction Counter() {\n  const dispatch = useAppDispatch();\n  return <button onClick={() => dispatch(increment())}>+1</button>;\n}",
    "relatedSlugs": [
      "redux-action",
      "redux-store",
      "thunk"
    ]
  },
  {
    "slug": "thunk",
    "label": "Thunk",
    "topic": "redux",
    "tooltip": "A function dispatched to the Redux store instead of a plain action object, used to handle async logic.",
    "definition": "A thunk is a function that wraps an expression to delay its evaluation — in Redux it is a function you dispatch that the `redux-thunk` middleware intercepts before it reaches the reducer. The thunk receives `(dispatch, getState)` and can perform async work (API calls, timeouts) then dispatch plain actions with the results. Redux Toolkit ships `createAsyncThunk` which auto-generates `pending`, `fulfilled`, and `rejected` action types and handles AbortController-based cancellation.",
    "codeExample": "import { createAsyncThunk } from '@reduxjs/toolkit';\n\nexport const fetchUser = createAsyncThunk(\n  'user/fetch',\n  async (userId: string) => {\n    const res = await fetch(`/api/users/${userId}`);\n    if (!res.ok) throw new Error('Not found');\n    return res.json();\n  },\n);\n\n// Dispatching from a component:\ndispatch(fetchUser('u_123'));",
    "relatedSlugs": [
      "redux-dispatch",
      "redux-middleware",
      "redux-saga"
    ]
  },
  {
    "slug": "redux-selector",
    "label": "Selector",
    "topic": "redux",
    "tooltip": "A function that reads and optionally derives data from the Redux store, optionally memoised with Reselect.",
    "definition": "A selector is a function that takes the Redux state and returns a derived value. Simple selectors are just arrow functions; complex ones use `createSelector` from Reselect (bundled with Redux Toolkit) to memoize the result — it only recomputes when its inputs change. Selectors decouple components from the store's internal shape: rename a field in state and only the selector changes, not every component that reads it.",
    "codeExample": "import { createSelector } from '@reduxjs/toolkit';\nimport type { RootState } from './store';\n\nconst selectItems  = (state: RootState) => state.cart.items;\nconst selectCoupon = (state: RootState) => state.cart.coupon;\n\nexport const selectTotal = createSelector(\n  [selectItems, selectCoupon],\n  (items, coupon) => {\n    const raw = items.reduce((s, i) => s + i.price * i.qty, 0);\n    return coupon ? raw * (1 - coupon.discount) : raw;\n  },\n);\n\n// In a component:\nconst total = useAppSelector(selectTotal);",
    "relatedSlugs": [
      "redux-store",
      "reselect"
    ]
  },
  {
    "slug": "redux-middleware",
    "label": "Redux Middleware",
    "topic": "redux",
    "tooltip": "A function that intercepts dispatched actions before they reach the reducer — used for async logic, logging, and crash reporting.",
    "definition": "Redux middleware is a higher-order function that wraps the `dispatch` function. It sits between `store.dispatch()` and the root reducer, letting you intercept, transform, delay, or side-effect on every action. The standard shape is `(store) => (next) => (action) => { ... }`. `redux-thunk` is the most common — it lets you dispatch functions. `redux-logger` logs state before and after. Multiple middlewares are composed left-to-right.",
    "codeExample": "// Custom middleware\nconst analyticsMiddleware =\n  (store: MiddlewareAPI) =>\n  (next: Dispatch) =>\n  (action: unknown) => {\n    if (isAction(action)) analytics.track(action.type);\n    return next(action);\n  };\n\n// Register in configureStore\nconfigureStore({\n  reducer: rootReducer,\n  middleware: (getDefault) =>\n    getDefault().concat(analyticsMiddleware),\n});",
    "relatedSlugs": [
      "thunk",
      "redux-saga",
      "redux-store"
    ]
  },
  {
    "slug": "redux-saga",
    "label": "redux-saga",
    "topic": "redux",
    "tooltip": "A middleware library that manages async side effects using ES6 generator functions and declarative effect descriptors.",
    "definition": "redux-saga is a Redux middleware that handles side effects — API calls, WebSocket messages, complex async orchestration — using ES6 generators. Instead of calling async functions directly, sagas `yield` plain effect descriptor objects (`call`, `put`, `take`, `race`, `fork`) that the middleware interprets. Because the generator only produces descriptions and never executes I/O itself, sagas are easy to test without mocks. Key helper effects: `takeEvery` (spawn a task per action), `takeLatest` (cancel previous task on new action), `race` (run effects in parallel, cancel the loser).",
    "codeExample": "import { call, put, takeLatest } from 'redux-saga/effects';\n\nfunction* fetchUserSaga(action: PayloadAction<string>) {\n  try {\n    const user: User = yield call(api.getUser, action.payload);\n    yield put(userLoaded(user));\n  } catch (e) {\n    yield put(userFailed(String(e)));\n  }\n}\n\nexport function* rootSaga() {\n  yield takeLatest('user/fetch', fetchUserSaga);\n}",
    "relatedSlugs": [
      "thunk",
      "redux-middleware"
    ]
  },
  {
    "slug": "flux",
    "label": "Flux",
    "topic": "redux",
    "tooltip": "Facebook's unidirectional data flow architecture — the pattern that directly inspired Redux.",
    "definition": "Flux is an application architecture pattern Facebook introduced alongside React in 2014 to tame the complexity of two-way data binding. It enforces a strict one-way cycle: Action → Dispatcher → Store → View. Redux was directly inspired by Flux but simplified it: it eliminates the separate Dispatcher, collapses multiple mutable stores into one immutable state tree, and replaces in-store mutation logic with pure reducer functions. Understanding Flux makes Redux's design decisions (one store, pure reducers, actions as the only mutation path) immediately obvious.",
    "codeExample": null,
    "relatedSlugs": [
      "redux-store",
      "redux-action",
      "redux-reducer"
    ]
  },
  {
    "slug": "redux-toolkit",
    "label": "Redux Toolkit",
    "topic": "redux",
    "tooltip": "The official, opinionated Redux toolset — configureStore, createSlice, createAsyncThunk, RTK Query.",
    "definition": "Redux Toolkit (RTK) is the official recommended way to write Redux logic. It wraps the raw Redux API with `configureStore` (auto-wires DevTools and thunk middleware), `createSlice` (generates reducers and action creators in one call), `createAsyncThunk` (handles pending/fulfilled/rejected lifecycle actions), and RTK Query (declarative data fetching and caching). It bundles Immer so you can write apparently-mutating reducer logic that actually produces immutable state, and Reselect for memoised selectors.",
    "codeExample": "import { configureStore, createSlice } from '@reduxjs/toolkit';\n\nconst counterSlice = createSlice({\n  name: 'counter',\n  initialState: { value: 0 },\n  reducers: {\n    increment: (state) => { state.value += 1; },\n  },\n});\n\nconst store = configureStore({\n  reducer: { counter: counterSlice.reducer },\n});\n\nexport const { increment } = counterSlice.actions;",
    "relatedSlugs": [
      "redux-store",
      "redux-reducer",
      "immer",
      "reselect"
    ]
  },
  {
    "slug": "immer",
    "label": "Immer",
    "topic": "redux",
    "tooltip": "A library that lets you write mutating-looking code that actually produces a new immutable state object.",
    "definition": "Immer uses JavaScript `Proxy` to record mutations you make on a draft copy of the state, then produces a new immutable object reflecting those changes — the original is never touched. Redux Toolkit bundles Immer into `createSlice` reducers, so you can write `state.items.push(item)` instead of `return { ...state, items: [...state.items, item] }`. Immer only applies inside `createSlice` (or `createReducer`) — mutating outside those functions is still a bug.",
    "codeExample": "// Without Immer — verbose spread\ncase 'addItem':\n  return {\n    ...state,\n    cart: { ...state.cart, items: [...state.cart.items, action.payload] },\n  };\n\n// With Immer (inside createSlice) — clear and direct\naddItem: (state, action: PayloadAction<CartItem>) => {\n  state.cart.items.push(action.payload); // safe — Immer wraps this\n},",
    "relatedSlugs": [
      "redux-reducer",
      "redux-toolkit"
    ]
  },
  {
    "slug": "reselect",
    "label": "Reselect",
    "topic": "redux",
    "tooltip": "A memoised selector library. createSelector only recomputes when its input selectors return new references.",
    "definition": "Reselect is the selector library bundled with Redux Toolkit. Its `createSelector` function takes one or more input selectors and a result function; the result function is only called when the inputs produce new values (by reference equality). This prevents React components from re-rendering when an expensive derived value hasn't actually changed. Selectors compose — pass one `createSelector` output as an input to another to build up a pipeline of cached transformations.",
    "codeExample": "import { createSelector } from '@reduxjs/toolkit';\n\nconst selectAllProducts = (state: RootState) => state.products.items;\nconst selectFilter      = (state: RootState) => state.products.filter;\n\n// Only recomputes when products or filter change\nexport const selectFilteredProducts = createSelector(\n  [selectAllProducts, selectFilter],\n  (products, filter) =>\n    filter\n      ? products.filter((p) => p.category === filter)\n      : products,\n);",
    "relatedSlugs": [
      "redux-selector",
      "redux-toolkit"
    ]
  },
  {
    "slug": "combine-reducers",
    "label": "combineReducers",
    "topic": "redux",
    "tooltip": "Utility that merges multiple slice reducers into one root reducer, each responsible for its own state slice.",
    "definition": "`combineReducers` (or the object shorthand in `configureStore`) composes multiple reducer functions into a single root reducer. Each slice reducer receives only its own piece of the state tree — it never sees or can accidentally affect sibling slices. On every dispatch, the root reducer calls each slice reducer with its slice and the action, then assembles the results into a new top-level state object. This is the primary mechanism for splitting Redux state by domain without requiring multiple stores.",
    "codeExample": "import { combineReducers } from '@reduxjs/toolkit';\n\n// Explicit combineReducers\nconst rootReducer = combineReducers({\n  cart:    cartReducer,\n  user:    userReducer,\n  session: sessionReducer,\n});\n\n// Or use the shorthand in configureStore (equivalent)\nconfigureStore({\n  reducer: {\n    cart:    cartReducer,\n    user:    userReducer,\n    session: sessionReducer,\n  },\n});",
    "relatedSlugs": [
      "redux-reducer",
      "redux-store"
    ]
  },
  {
    "slug": "closure",
    "label": "Closure",
    "topic": "javascript",
    "tooltip": "A function that retains access to variables from its outer scope even after that scope has finished executing.",
    "definition": "A closure is formed when a function is defined inside another function and the inner function captures (closes over) variables from the outer function's scope. The inner function retains a live reference to those variables — not a copy — so it reads and writes the same memory even after the outer function has returned. Closures are the mechanism behind data privacy (module pattern), partial application, memoization, and any code that needs to 'remember' state between calls without using global variables.",
    "codeExample": "function makeCounter(start = 0) {\n  let count = start; // captured by the closure\n  return {\n    increment: () => ++count,\n    decrement: () => --count,\n    value:     () => count,\n  };\n}\n\nconst counter = makeCounter(10);\ncounter.increment(); // 11\ncounter.increment(); // 12\ncounter.value();     // 12\n// count is private — unreachable outside makeCounter",
    "relatedSlugs": [
      "scope",
      "iife",
      "module-pattern-js"
    ]
  },
  {
    "slug": "scope",
    "label": "Scope",
    "topic": "javascript",
    "tooltip": "The region of code where a variable is accessible. JavaScript has global, function, and block scope.",
    "definition": "Scope determines where in the code a variable can be read or written. JavaScript has three scope types: **global scope** (accessible everywhere), **function scope** (`var` declarations are limited to the enclosing function), and **block scope** (`let` and `const` are limited to the nearest `{}`). Scope is lexical — it is determined by where you write the code, not where it is called from. Nested functions form a scope chain: inner scopes can access outer variables, but outer scopes cannot access inner variables.",
    "codeExample": "var globalVar = 'global';\n\nfunction outer() {\n  let outerLet = 'outer';\n\n  function inner() {\n    let innerLet = 'inner';\n    console.log(outerLet);  // ✅ accessible via scope chain\n    console.log(globalVar); // ✅ accessible\n  }\n\n  inner();\n  // console.log(innerLet); // ❌ ReferenceError\n}\n\n// Block scope\n{\n  let blockVar = 'block';\n}\n// console.log(blockVar); // ❌ ReferenceError",
    "relatedSlugs": [
      "closure",
      "hoisting-js"
    ]
  },
  {
    "slug": "hoisting-js",
    "label": "Hoisting",
    "topic": "javascript",
    "tooltip": "JavaScript's behaviour of moving var declarations and function definitions to the top of their scope before execution.",
    "definition": "Hoisting is the mechanism by which JavaScript's engine processes declarations before executing code. `var` declarations are hoisted and initialised to `undefined`, so they can be referenced before the line they appear on (returning `undefined`, not an error). Function declarations are fully hoisted — both name and body — so they can be called before their definition. `let` and `const` are hoisted to the top of their block but placed in a Temporal Dead Zone (TDZ) — accessing them before the declaration line throws a `ReferenceError`. Only declarations are hoisted, not assignments.",
    "codeExample": "// var — hoisted as undefined\nconsole.log(x); // undefined (no error)\nvar x = 5;\n\n// function declaration — fully hoisted\ngreet(); // 'Hello' — works before definition\nfunction greet() { return 'Hello'; }\n\n// let/const — TDZ\nconsole.log(y); // ReferenceError!\nlet y = 10;",
    "relatedSlugs": [
      "scope",
      "closure"
    ]
  },
  {
    "slug": "iife",
    "label": "IIFE",
    "topic": "javascript",
    "tooltip": "Immediately Invoked Function Expression — a function that executes the moment it is defined, creating a private scope.",
    "definition": "An IIFE (Immediately Invoked Function Expression) is a function expression wrapped in parentheses and immediately called. The wrapping parentheses make it an expression rather than a declaration; the trailing `()` invokes it straight away. IIFEs were the primary way to create private scope before ES6 modules — any `var` inside the IIFE is invisible to the outside world. Today, ES modules provide file-level scope, making IIFEs mostly a legacy pattern, though they still appear in bundled output and libraries that need to support environments without a module system.",
    "codeExample": "(function () {\n  var privateVar = 'hidden';\n  console.log('IIFE ran');\n})();\n\n// Arrow function variant\n(() => {\n  const init = 'also private';\n  console.log(init);\n})();\n\n// console.log(privateVar); // ❌ ReferenceError",
    "relatedSlugs": [
      "closure",
      "scope"
    ]
  },
  {
    "slug": "event-loop",
    "label": "Event Loop",
    "topic": "javascript",
    "tooltip": "The mechanism that allows JavaScript to perform non-blocking operations by offloading work and processing queued callbacks one at a time.",
    "definition": "JavaScript is single-threaded — only one piece of code runs at a time. The event loop is the scheduler that coordinates what runs next. It works with three areas: the **call stack** (currently executing code), the **microtask queue** (Promise callbacks, `queueMicrotask`), and the **macrotask queue** (`setTimeout`, I/O, UI events). After each call stack is emptied, the event loop drains the entire microtask queue first, then picks one macrotask. This ordering is why Promise `.then` callbacks always run before `setTimeout(fn, 0)` callbacks.",
    "codeExample": "console.log('1');\nsetTimeout(() => console.log('4'), 0); // macrotask\nPromise.resolve().then(() => console.log('3')); // microtask\nconsole.log('2');\n// Output: 1 → 2 → 3 → 4",
    "relatedSlugs": [
      "promise-js",
      "async-await"
    ]
  },
  {
    "slug": "promise-js",
    "label": "Promise",
    "topic": "javascript",
    "tooltip": "An object representing the eventual completion or failure of an asynchronous operation, enabling cleaner async code than callbacks.",
    "definition": "A Promise is an object that represents the result of an asynchronous operation that will settle at some future point. It has three states: **pending** (initial), **fulfilled** (succeeded with a value), and **rejected** (failed with a reason). Once settled it is immutable. Handlers are registered with `.then(onFulfilled)`, `.catch(onRejected)`, and `.finally(onSettled)`. Each `.then` returns a new Promise, enabling chaining. Promise callbacks are microtasks — they run before the next event loop macrotask. `Promise.all`, `Promise.allSettled`, `Promise.race`, and `Promise.any` coordinate multiple concurrent promises.",
    "codeExample": "const fetchUser = (id) =>\n  fetch(`/api/users/${id}`)\n    .then(res => {\n      if (!res.ok) throw new Error(`HTTP ${res.status}`);\n      return res.json();\n    });\n\nfetchUser(1)\n  .then(user => console.log(user.name))\n  .catch(err => console.error(err))\n  .finally(() => console.log('done'));",
    "relatedSlugs": [
      "async-await",
      "event-loop"
    ]
  },
  {
    "slug": "async-await",
    "label": "async / await",
    "topic": "javascript",
    "tooltip": "Syntactic sugar over Promises that makes asynchronous code read like synchronous code.",
    "definition": "`async` functions always return a Promise. `await` suspends the function's execution until the awaited Promise settles, then resumes with the resolved value. Under the hood, the engine transforms the async function into a state machine similar to a generator. The suspended function does not block the thread — other tasks continue to run. Error handling uses `try/catch` rather than `.catch()`. Independent async operations should be started simultaneously with `Promise.all` rather than awaited sequentially, to avoid unnecessary latency.",
    "codeExample": "async function loadDashboard(userId) {\n  try {\n    // Parallel — both requests fire at the same time\n    const [user, posts] = await Promise.all([\n      fetchUser(userId),\n      fetchPosts(userId),\n    ]);\n    return { user, posts };\n  } catch (err) {\n    console.error('Dashboard load failed:', err);\n    throw err;\n  }\n}",
    "relatedSlugs": [
      "promise-js",
      "event-loop"
    ]
  },
  {
    "slug": "prototype-chain",
    "label": "Prototype Chain",
    "topic": "javascript",
    "tooltip": "The linked chain of objects JavaScript searches when looking up a property that doesn't exist on the object itself.",
    "definition": "Every JavaScript object has an internal `[[Prototype]]` link pointing to another object (or `null`). When you access a property, the engine first checks the object itself. If not found, it follows the `[[Prototype]]` link and checks that object — and so on up the chain until it finds the property or reaches `null` (the chain's end). This is how objects can 'inherit' methods without copying them: they all share a single copy on a prototype object. ES6 `class` and `extends` use this same mechanism.",
    "codeExample": "const animal = { breathe() { return 'breathing'; } };\nconst dog = Object.create(animal);\ndog.bark = () => 'woof';\n\nconst rex = Object.create(dog);\nrex.bark();    // 'woof'    — found on dog\nrex.breathe(); // 'breathing' — found on animal\n\n// Check own vs inherited\nrex.hasOwnProperty('bark');    // false\ndog.hasOwnProperty('bark');    // true",
    "relatedSlugs": [
      "closure",
      "scope"
    ]
  },
  {
    "slug": "event-delegation",
    "label": "Event Delegation",
    "topic": "javascript",
    "tooltip": "Attaching a single event listener to a parent element to handle events from all its children via bubbling.",
    "definition": "Event delegation exploits event bubbling: instead of attaching a listener to every child element, you attach one listener to a common parent. When a child is clicked, the event bubbles up and the parent's handler fires. You use `event.target` (or `event.target.closest(selector)`) to identify which child triggered it. Benefits: fewer listeners (less memory), automatically covers dynamically added children, single cleanup point on teardown. It is especially valuable for large lists, tables, and components that mount/unmount items frequently.",
    "codeExample": "const list = document.querySelector('#todo-list');\n\nlist.addEventListener('click', (e) => {\n  const item = e.target.closest('[data-id]');\n  if (!item) return;\n\n  if (e.target.matches('.delete-btn')) {\n    deleteItem(item.dataset.id);\n  } else if (e.target.matches('.complete-btn')) {\n    toggleComplete(item.dataset.id);\n  }\n});\n// Works for items added later — no re-binding needed",
    "relatedSlugs": [
      "event-loop"
    ]
  },
  {
    "slug": "debounce",
    "label": "Debounce",
    "topic": "javascript",
    "tooltip": "A technique that delays invoking a function until after a specified quiet period since the last call.",
    "definition": "Debouncing ensures a function only executes after a specified delay has elapsed since the last invocation. Every new call resets the timer. It is used for events that fire rapidly where you only care about the final settled state — search input (wait until the user stops typing), window resize, autocomplete, form validation. The delay is typically 150–300 ms for user input. A leading variant fires immediately on the first call and then ignores subsequent calls during the quiet period.",
    "codeExample": "function debounce(fn, delay) {\n  let timerId;\n  return function (...args) {\n    clearTimeout(timerId);\n    timerId = setTimeout(() => fn.apply(this, args), delay);\n  };\n}\n\nconst handleSearch = debounce((query) => {\n  fetchResults(query);\n}, 300);\n\ninput.addEventListener('input', e => handleSearch(e.target.value));\n// API is only called 300ms after the user stops typing",
    "relatedSlugs": [
      "throttle",
      "event-loop"
    ]
  },
  {
    "slug": "throttle",
    "label": "Throttle",
    "topic": "javascript",
    "tooltip": "A technique that limits a function to fire at most once per specified interval, regardless of how often it is called.",
    "definition": "Throttling ensures a function executes at most once per specified time interval, no matter how frequently the triggering event fires. Unlike debounce (which waits for silence), throttle guarantees regular execution during continuous activity. Common use cases: scroll and resize handlers (limit to ~60fps), API rate limiting, game input processing. A leading throttle fires on the first call; a trailing throttle fires after the interval; most implementations fire on both.",
    "codeExample": "function throttle(fn, interval) {\n  let lastTime = 0;\n  return function (...args) {\n    const now = Date.now();\n    if (now - lastTime >= interval) {\n      lastTime = now;\n      fn.apply(this, args);\n    }\n  };\n}\n\nconst onScroll = throttle(updateParallax, 16); // ~60fps\nwindow.addEventListener('scroll', onScroll);",
    "relatedSlugs": [
      "debounce",
      "event-loop"
    ]
  },
  {
    "slug": "generator-js",
    "label": "Generator",
    "topic": "javascript",
    "tooltip": "A function that can pause its execution with yield and resume later, producing a sequence of values on demand.",
    "definition": "A generator function (`function*`) returns an iterator when called. Inside the body, `yield` pauses execution and emits a value; calling `.next()` resumes from where it left off. Generators enable lazy evaluation — values are produced only when requested, which is memory-efficient for large or infinite sequences. They also support two-way communication: `yield` expressions can receive values passed via `.next(value)`. Redux-saga uses generators to orchestrate async side effects. `async/await` was itself inspired by the generator + Promise pattern.",
    "codeExample": "function* range(start, end) {\n  for (let i = start; i <= end; i++) {\n    yield i;\n  }\n}\n\nfor (const n of range(1, 5)) {\n  console.log(n); // 1 2 3 4 5\n}\n\n// Infinite sequence — only computes on demand\nfunction* naturals() {\n  let n = 1;\n  while (true) yield n++;\n}\nconst gen = naturals();\ngen.next().value; // 1\ngen.next().value; // 2",
    "relatedSlugs": [
      "event-loop",
      "async-await"
    ]
  },
  {
    "slug": "symbol-js",
    "label": "Symbol",
    "topic": "javascript",
    "tooltip": "A primitive value that is guaranteed to be unique every time it is created, used as collision-free property keys.",
    "definition": "Symbol is a primitive type introduced in ES6. Every `Symbol()` call produces a unique value — even if you pass the same description string. Symbols are non-enumerable by default, so they don't appear in `Object.keys()` or `for...in` loops, making them ideal for attaching metadata without polluting the public API. Well-known symbols like `Symbol.iterator`, `Symbol.toPrimitive`, and `Symbol.hasInstance` let objects hook into built-in JavaScript behaviours. `Symbol.for(key)` creates symbols in a global registry shared across modules.",
    "codeExample": "const ID = Symbol('id');\nconst user = { name: 'Alice', [ID]: 42 };\n\nconsole.log(Object.keys(user)); // ['name'] — Symbol hidden\nconsole.log(user[ID]);          // 42\n\n// Well-known symbol: custom iteration\nclass Range {\n  constructor(a, b) { this.a = a; this.b = b; }\n  [Symbol.iterator]() {\n    let i = this.a;\n    return { next: () => i <= this.b ? { value: i++, done: false } : { done: true } };\n  }\n}\nfor (const n of new Range(1, 3)) console.log(n); // 1 2 3",
    "relatedSlugs": [
      "prototype-chain"
    ]
  },
  {
    "slug": "web-worker",
    "label": "Web Worker",
    "topic": "javascript",
    "tooltip": "A background thread in the browser that runs JavaScript without blocking the main UI thread.",
    "definition": "Web Workers run JavaScript in a separate thread from the main (UI) thread. Because JavaScript is single-threaded by default, CPU-heavy work blocks the event loop and freezes the UI. Web Workers solve this by offloading computation. Workers communicate with the main thread via `postMessage` / `onmessage` (using the structured-clone algorithm or Transferable objects for zero-copy buffer transfers). Limitations: no DOM access, no `window` or `document`. They can use `fetch`, `IndexedDB`, `WebSocket`, and `WebAssembly`. The `comlink` library wraps workers in a Proxy, eliminating most message-passing boilerplate.",
    "codeExample": "// main.js\nconst worker = new Worker('/worker.js');\nworker.postMessage({ data: largeArray });\nworker.onmessage = (e) => console.log('Result:', e.data.result);\n\n// worker.js\nself.onmessage = (e) => {\n  const result = e.data.data.reduce((a, b) => a + b, 0);\n  self.postMessage({ result });\n};",
    "relatedSlugs": [
      "event-loop"
    ]
  },
  {
    "slug": "unit-test",
    "label": "Unit Test",
    "topic": "unit-testing",
    "tooltip": "A test that verifies a single function or component in isolation, with all dependencies replaced by mocks.",
    "definition": "A unit test exercises the smallest testable piece of code — typically a single function, method, or component — in complete isolation from external systems. Dependencies such as databases, APIs, and the file system are replaced with mocks or stubs. Unit tests run in milliseconds, are fully deterministic, and pinpoint failures to a specific unit of logic. In JavaScript, Jest is the dominant framework. In React, React Testing Library is used to test component behaviour from the user's perspective without coupling tests to implementation details.",
    "codeExample": "// Pure function unit test\ntest('formatPrice returns £ prefixed string', () => {\n  expect(formatPrice(9.99)).toBe('£9.99');\n});\n\n// React component unit test\ntest('shows error message on failed submit', async () => {\n  render(<LoginForm onSubmit={jest.fn().mockRejectedValue(new Error('Bad credentials'))} />);\n  await userEvent.click(screen.getByRole('button', { name: /sign in/i }));\n  expect(await screen.findByRole('alert')).toHaveTextContent('Bad credentials');\n});",
    "relatedSlugs": [
      "mock",
      "test-double",
      "arrange-act-assert",
      "integration-test"
    ]
  },
  {
    "slug": "integration-test",
    "label": "Integration Test",
    "topic": "unit-testing",
    "tooltip": "A test that verifies multiple units work correctly together, often with real (or near-real) dependencies.",
    "definition": "Integration tests check the contracts between units — where unit tests guarantee each piece works in isolation, integration tests guarantee they work together. They may use a real database, a real HTTP server, or both. In frontend development, integration tests (using React Testing Library) typically render a feature with real child components but mocked API calls. On the backend, integration tests usually hit a test database seeded with known fixtures. Integration tests are slower than unit tests but catch bugs that only appear at the seams: wrong SQL queries, middleware misconfiguration, broken API contracts.",
    "codeExample": "// Backend integration test (API + test DB)\ntest('POST /api/users creates a user and returns 201', async () => {\n  const res = await request(app)\n    .post('/api/users')\n    .send({ name: 'Aaron', email: 'aaron@example.com' });\n\n  expect(res.status).toBe(201);\n  const user = await db.user.findUnique({ where: { email: 'aaron@example.com' } });\n  expect(user).not.toBeNull();\n});",
    "relatedSlugs": [
      "unit-test",
      "mock",
      "test-double"
    ]
  },
  {
    "slug": "mock",
    "label": "Mock",
    "topic": "unit-testing",
    "tooltip": "A controlled fake that replaces a real dependency and records calls so you can assert on them.",
    "definition": "A mock replaces a real dependency with a controlled substitute that returns predetermined values and records how it was called. Unlike a stub (which only returns values), a mock also lets you assert that your code called it with the right arguments, the right number of times, and in the right order. In Jest, `jest.fn()` creates a mock function and `jest.mock()` replaces an entire module. Mocks are essential for testing code that depends on external systems (databases, APIs, email services) without actually involving them. Over-mocking is a smell — if you mock everything, you end up testing the mocks, not the behaviour.",
    "codeExample": "// jest.fn() — mock function\nconst sendEmail = jest.fn().mockResolvedValue({ success: true });\n\nawait notifyUser('aaron@example.com');\n\nexpect(sendEmail).toHaveBeenCalledWith('aaron@example.com', expect.any(String));\nexpect(sendEmail).toHaveBeenCalledTimes(1);\n\n// jest.mock() — replace an entire module\njest.mock('@/lib/email', () => ({\n  sendEmail: jest.fn().mockResolvedValue({ success: true }),\n}));",
    "relatedSlugs": [
      "spy",
      "test-double",
      "unit-test"
    ]
  },
  {
    "slug": "spy",
    "label": "Spy",
    "topic": "unit-testing",
    "tooltip": "A test double that wraps the real implementation, letting it run while recording calls for later assertion.",
    "definition": "A spy wraps the real implementation of a function or method. The original code still executes, but the spy records every call — arguments, return values, and call count — so you can assert on them. This contrasts with a mock, which replaces the real implementation entirely. In Jest, `jest.spyOn(object, 'methodName')` creates a spy. You can optionally override the implementation with `.mockImplementation()`, effectively turning the spy into a mock. Always call `.mockRestore()` after the test to put the original implementation back. Spies are ideal when you want to verify that a collaborator was called without suppressing its real behaviour (e.g., checking a logger was invoked).",
    "codeExample": "// Spy — real implementation still runs\nconst consoleSpy = jest.spyOn(console, 'warn');\n\nvalidateInput(null); // internally calls console.warn\n\nexpect(consoleSpy).toHaveBeenCalledWith(expect.stringContaining('null input'));\nconsoleSpy.mockRestore();\n\n// Spy with override\nconst fetchSpy = jest.spyOn(api, 'fetchUser').mockResolvedValue({ name: 'Aaron' });\n// Real fetchUser NOT called — spy overrides it\nfetchSpy.mockRestore();",
    "relatedSlugs": [
      "mock",
      "test-double"
    ]
  },
  {
    "slug": "tdd",
    "label": "TDD",
    "topic": "unit-testing",
    "tooltip": "Test-Driven Development — a practice where you write a failing test before writing the implementation code.",
    "definition": "Test-Driven Development (TDD) is a development workflow where you write a failing test first, then write just enough code to make it pass, then refactor — repeating in short cycles. This is often called Red-Green-Refactor. TDD drives API design from the caller's perspective (you write the test before the implementation exists, so you design the API to be easy to use). Benefits include better-designed code, a built-in regression safety net, and tests that document intent. Drawbacks include slower initial velocity, friction with exploratory/UI work, and a tendency toward over-mocking if not applied carefully.",
    "codeExample": "// 1. RED — write a failing test\ntest('calculateVAT adds 20% to the price', () => {\n  expect(calculateVAT(100)).toBe(120); // calculateVAT doesn't exist yet\n});\n\n// 2. GREEN — write just enough to pass\nfunction calculateVAT(price: number) {\n  return price * 1.2;\n}\n\n// 3. REFACTOR — clean up\nconst VAT_RATE = 0.2;\nfunction calculateVAT(price: number) {\n  return price * (1 + VAT_RATE);\n}",
    "relatedSlugs": [
      "unit-test",
      "arrange-act-assert",
      "code-coverage"
    ]
  },
  {
    "slug": "arrange-act-assert",
    "label": "Arrange-Act-Assert",
    "topic": "unit-testing",
    "tooltip": "A pattern for structuring unit tests into three distinct phases: setup, execution, and verification.",
    "definition": "Arrange-Act-Assert (AAA) is the standard pattern for structuring tests. Arrange sets up the system under test and its dependencies. Act calls the function or triggers the behaviour being tested — there should be exactly one Act per test. Assert verifies the outcome. The pattern makes tests readable and enforces single responsibility. It maps directly to BDD's Given-When-Then. In React Testing Library: render = Arrange, userEvent/fireEvent = Act, expect = Assert. A test with multiple Acts usually means it's testing more than one thing and should be split.",
    "codeExample": "test('applying a coupon reduces the total', async () => {\n  // ARRANGE\n  const user = userEvent.setup();\n  render(<Cart items={[{ price: 100 }]} />);\n\n  // ACT\n  await user.type(screen.getByLabelText(/coupon/i), 'SAVE10');\n  await user.click(screen.getByRole('button', { name: /apply/i }));\n\n  // ASSERT\n  expect(screen.getByText(/total: £90/i)).toBeInTheDocument();\n});",
    "relatedSlugs": [
      "unit-test",
      "tdd"
    ]
  },
  {
    "slug": "code-coverage",
    "label": "Code Coverage",
    "topic": "unit-testing",
    "tooltip": "A metric that measures the percentage of your codebase exercised by tests — split into line, branch, and function coverage.",
    "definition": "Code coverage measures how much of your source code is executed when tests run. The main types are: line coverage (% of lines executed), branch coverage (% of conditional branches — if/else, ternary — taken), function coverage (% of functions called), and statement coverage (% of statements executed). Branch coverage is the most useful because it catches untested conditional paths. The industry sweet spot is 80–85%. Chasing 100% produces diminishing returns — you end up testing trivial getters and defensive checks. Coverage is a lagging indicator, not a quality metric; a test that achieves 100% coverage without asserting meaningful outcomes is worthless.",
    "codeExample": "// jest.config.js\nmodule.exports = {\n  collectCoverage: true,\n  coverageThreshold: {\n    global: {\n      branches: 80,\n      functions: 85,\n      lines: 85,\n    },\n  },\n};\n\n// Run: npx jest --coverage\n// Output:\n// Statements   : 92% (230/250)\n// Branches     : 81% (65/80)\n// Functions    : 95% (57/60)\n// Lines        : 91% (228/250)",
    "relatedSlugs": [
      "unit-test",
      "tdd",
      "arrange-act-assert"
    ]
  },
  {
    "slug": "test-double",
    "label": "Test Double",
    "topic": "unit-testing",
    "tooltip": "An umbrella term for any object that stands in for a real dependency in a test — includes mocks, stubs, spies, fakes, and dummies.",
    "definition": "Test double is the umbrella term (coined by Gerard Meszaros) for any object that replaces a real dependency in a test. The five subtypes are: Dummy (passed but never used — fills a parameter list), Stub (returns hardcoded values), Mock (records interactions and can assert on them), Spy (wraps real implementation, records calls), and Fake (a working simplified implementation — e.g. an in-memory database). In everyday JavaScript/TypeScript usage the terms mock and stub are often used loosely to mean any test double, but understanding the distinctions helps you choose the right tool.",
    "codeExample": "// Dummy — just fills the parameter, never used\nconst dummyLogger = {} as Logger;\n\n// Stub — returns hardcoded value\nconst stubRepo = { findById: jest.fn().mockResolvedValue({ id: '1', name: 'Aaron' }) };\n\n// Mock — records + can assert\nconst mockEmail = jest.fn();\nawait service.run(); \nexpect(mockEmail).toHaveBeenCalledTimes(1);\n\n// Fake — working in-memory implementation\nclass InMemoryUserRepo implements UserRepository {\n  private users: User[] = [];\n  async findById(id: string) { return this.users.find(u => u.id === id) ?? null; }\n  async save(user: User) { this.users.push(user); return user; }\n}",
    "relatedSlugs": [
      "mock",
      "spy",
      "unit-test"
    ]
  },
  {
    "slug": "singleton",
    "label": "Singleton",
    "topic": "design-patterns",
    "tooltip": "A creational pattern that ensures a class has only one instance and provides a global access point to it.",
    "definition": "The Singleton pattern restricts instantiation of a class to a single object. In JavaScript, ES module exports are natural singletons — every import returns the same module-level object. Common uses: a Redux store, a Supabase client, an app-wide logger. Singletons can be problematic in tests because they carry state between tests and can't be easily replaced with mocks. Prefer dependency injection over ambient singletons when testability matters.",
    "codeExample": "// Natural JS singleton via module export\nimport { createClient } from '@supabase/supabase-js';\nexport const supabase = createClient(URL, KEY);\n// Every import gets the same instance\n\n// Classic class singleton\nclass Logger {\n  private static instance: Logger;\n  private constructor() {}\n  static getInstance() {\n    if (!Logger.instance) Logger.instance = new Logger();\n    return Logger.instance;\n  }\n}",
    "relatedSlugs": [
      "design-pattern",
      "dependency-injection"
    ]
  },
  {
    "slug": "observer-pattern",
    "label": "Observer",
    "topic": "design-patterns",
    "tooltip": "A behavioral pattern where a subject notifies all subscribed observers automatically when its state changes.",
    "definition": "The Observer pattern defines a one-to-many dependency between objects. When the subject (publisher) changes state, all registered observers (subscribers) are notified. It is the foundation of event-driven programming. In the browser: `addEventListener` is the Observer pattern. In Node.js: `EventEmitter`. In React: the Context API re-renders consumers when context value changes; Zustand re-renders components subscribed to a state slice. The key benefit is decoupling — the subject doesn't need to know anything about its observers beyond the notification interface.",
    "codeExample": "// DOM Observer\nwindow.addEventListener('resize', handleResize);\n\n// Zustand — components are observers of store slices\nconst count = useCountStore((state) => state.count);\n// Component re-renders only when count changes\n\n// Custom event emitter\nconst emitter = new EventEmitter();\nconst unsubscribe = emitter.subscribe({ update: (data) => console.log(data) });\nemitter.emit({ type: 'USER_LOGIN', user });\nunsubscribe(); // clean up",
    "relatedSlugs": [
      "strategy-pattern",
      "command-pattern"
    ]
  },
  {
    "slug": "strategy-pattern",
    "label": "Strategy",
    "topic": "design-patterns",
    "tooltip": "A behavioral pattern that defines a family of interchangeable algorithms and lets you swap them at runtime.",
    "definition": "The Strategy pattern encapsulates a family of algorithms behind a common interface, making them interchangeable without modifying the context that uses them. This eliminates long if/else or switch chains and follows the Open/Closed Principle — adding a new algorithm means adding a new strategy, not editing existing code. In JavaScript, strategies are often plain functions stored in a map. In React, custom hooks that accept a strategy function (a sort function, a validation function) are the idiomatic form.",
    "codeExample": "// Functional strategies (JS-idiomatic)\nconst sortStrategies = {\n  alphabetical: (items) => [...items].sort((a, b) => a.name.localeCompare(b.name)),\n  priceAsc: (items) => [...items].sort((a, b) => a.price - b.price),\n};\n\nfunction sortProducts(products, strategy) {\n  return sortStrategies[strategy](products);\n}\n\n// React: hook accepts a strategy\nfunction useField(validate) {\n  const [value, setValue] = useState('');\n  return { value, setValue, isValid: validate(value) };\n}\nconst email = useField(v => /^[^@]+@[^@]+/.test(v));",
    "relatedSlugs": [
      "observer-pattern",
      "command-pattern",
      "design-pattern"
    ]
  },
  {
    "slug": "decorator-pattern",
    "label": "Decorator",
    "topic": "design-patterns",
    "tooltip": "A structural pattern that wraps an object to add behaviour without modifying the original class.",
    "definition": "The Decorator pattern attaches additional responsibilities to an object dynamically by wrapping it in another object with the same interface. It is an alternative to subclassing and follows the Open/Closed Principle. In JavaScript, Higher-Order Functions are functional decorators. In React, Higher-Order Components (HOCs) like `withAuth()` and `withErrorBoundary()` are classic decorators — they wrap a component and return an enhanced version. The TC39 decorators proposal (`@decorator`) brings the pattern to class syntax.",
    "codeExample": "// HOC Decorator in React\nfunction withAuth(Component) {\n  return function AuthGuard(props) {\n    const { user } = useAuth();\n    if (!user) return <Navigate to=\"/login\" />;\n    return <Component {...props} />;\n  };\n}\nconst ProtectedDashboard = withAuth(Dashboard);\n\n// Function decorator (HOF)\nconst memoize = (fn) => {\n  const cache = new Map();\n  return (...args) => {\n    const key = JSON.stringify(args);\n    if (!cache.has(key)) cache.set(key, fn(...args));\n    return cache.get(key);\n  };\n};",
    "relatedSlugs": [
      "design-pattern",
      "facade-pattern",
      "adapter-pattern"
    ]
  },
  {
    "slug": "facade-pattern",
    "label": "Facade",
    "topic": "design-patterns",
    "tooltip": "A structural pattern that provides a simplified interface to a complex subsystem.",
    "definition": "The Facade pattern hides the complexity of a subsystem behind a clean, simple API. Callers interact with the facade rather than coordinating multiple low-level components themselves. In frontend, API service layers are classic facades — `authService.login()` hides fetch calls, token storage, header management, and error normalisation. Custom hooks that wrap TanStack Query (e.g. `useProducts()`) are facades that hide query keys, cache config, and data transformation. Facades don't add new capabilities — they simplify access to existing ones.",
    "codeExample": "// API service facade\nclass AuthService {\n  async login(email, password) {\n    // hides: fetch, JSON parse, token storage,\n    // refresh scheduling, error normalisation\n    const { token, user } = await this.post('/auth/login', { email, password });\n    this.tokenStore.save(token);\n    this.refreshScheduler.schedule(token);\n    return user;\n  }\n}\n\n// Hook facade\nfunction useProducts(filters) {\n  return useQuery({\n    queryKey: ['products', filters],\n    queryFn: () => api.fetchProducts(filters),\n    select: (d) => d.items.map(normalise),\n  });\n}",
    "relatedSlugs": [
      "adapter-pattern",
      "decorator-pattern",
      "design-pattern"
    ]
  },
  {
    "slug": "adapter-pattern",
    "label": "Adapter",
    "topic": "design-patterns",
    "tooltip": "A structural pattern that converts one interface into another that clients expect, making incompatible interfaces work together.",
    "definition": "The Adapter pattern wraps an object with an incompatible interface and exposes the interface clients expect — like a power adapter for electrical sockets. In frontend, adapters normalise third-party API responses into the shape your app uses, wrap a library being replaced (so call sites don't need to change), or unify data from multiple sources into a consistent format. Adapters are distinct from facades: a facade simplifies complexity inside your system; an adapter translates an external interface into your system's expected shape.",
    "codeExample": "// Normalise third-party API response\nfunction adaptLegacyUser(raw) {\n  return {\n    fullName: `${raw.first_name} ${raw.last_name}`,\n    email: raw.email_address,\n    active: raw.status === 'active',\n  };\n}\nconst users = rawData.map(adaptLegacyUser);\n\n// Library migration adapter — call sites unchanged\nclass FetchAdapter {\n  async get(url, config) {\n    const res = await fetch(url + (config?.params ? '?' + new URLSearchParams(config.params) : ''));\n    if (!res.ok) throw new Error(res.statusText);\n    return res.json();\n  }\n}",
    "relatedSlugs": [
      "facade-pattern",
      "decorator-pattern",
      "design-pattern"
    ]
  },
  {
    "slug": "dependency-injection",
    "label": "Dependency Injection",
    "topic": "design-patterns",
    "tooltip": "A technique where a class receives its dependencies from the outside rather than creating them itself.",
    "definition": "Dependency Injection (DI) is a technique where dependencies are passed to a class or function rather than created internally. This makes the dependency explicit (visible in the function signature), easy to swap (pass a mock in tests), and decoupled (the class doesn't know or care which concrete implementation it gets). In React, DI is idiomatic: props inject dependencies into components, Context provides them to subtrees, and custom hooks can accept repositories/services as arguments. DI is the preferred alternative to the Service Locator pattern, which hides dependencies behind a global registry.",
    "codeExample": "// Constructor DI\nclass OrderService {\n  constructor(\n    private repo: OrderRepository,\n    private email: EmailService\n  ) {}\n}\n// Production\nconst svc = new OrderService(new SupabaseRepo(), new SendGridEmail());\n// Tests\nconst svc = new OrderService(mockRepo, mockEmail);\n\n// DI via React props\nfunction UserCard({ user, onEdit }) {\n  return <button onClick={() => onEdit(user.id)}>{user.name}</button>;\n}\n\n// DI via hook argument\nfunction useUserService(repo = defaultRepo) {\n  return { getUser: (id) => repo.findById(id) };\n}",
    "relatedSlugs": [
      "singleton",
      "facade-pattern",
      "design-pattern"
    ]
  },
  {
    "slug": "design-pattern",
    "label": "Design Pattern",
    "topic": "design-patterns",
    "tooltip": "A named, reusable solution to a commonly recurring problem in software design.",
    "definition": "Design patterns are battle-tested, named templates for solving recurring software design problems. They originate from the Gang of Four (GoF) book *Design Patterns* (1994), which catalogued 23 patterns in three categories: Creational (how objects are created — Singleton, Factory, Builder), Structural (how objects are composed — Adapter, Facade, Decorator, Proxy, Composite), and Behavioral (how objects communicate — Observer, Strategy, Command, State). Patterns give teams a shared vocabulary and encode decades of collective design experience. They are templates — not code to copy. Over-applying patterns where simpler solutions exist is a common mistake.",
    "codeExample": "// Three categories in one example\n\n// Creational — Factory decides which class to create\nconst processor = createPaymentProcessor(process.env.PROVIDER);\n\n// Structural — Facade simplifies a complex subsystem\nconst user = await authService.login(email, password);\n\n// Behavioral — Observer decouples event source from handlers\nwindow.addEventListener('resize', updateLayout);\nstore.subscribe(renderUI);",
    "relatedSlugs": [
      "singleton",
      "observer-pattern",
      "strategy-pattern",
      "decorator-pattern",
      "facade-pattern",
      "adapter-pattern"
    ]
  },
  {
    "slug": "css-box-model",
    "label": "Box Model",
    "topic": "css",
    "tooltip": "Every element is a rectangular box made up of content, padding, border, and margin — in that order.",
    "definition": "The CSS box model describes the rectangular box generated for every element in the document. From inside out: content area (width × height), padding (space between content and border), border (the visible edge), and margin (transparent space separating the element from its neighbours). By default (box-sizing: content-box), width and height set only the content area, so adding padding and border increases the rendered size. box-sizing: border-box (the modern default via a CSS reset) makes width/height include padding and border, which is almost always what you want.",
    "codeExample": "/* border-box makes width predictable */\n*, *::before, *::after {\n  box-sizing: border-box;\n}\n\n.card {\n  width: 300px;     /* total width is always 300px */\n  padding: 16px;\n  border: 1px solid #ccc;\n  /* content area = 300 - 32 - 2 = 266px */\n}",
    "relatedSlugs": [
      "css-specificity",
      "css-flexbox"
    ]
  },
  {
    "slug": "css-specificity",
    "label": "Specificity",
    "topic": "css",
    "tooltip": "The algorithm browsers use to decide which CSS rule wins when multiple rules target the same element.",
    "definition": "Specificity is calculated as a four-part score — inline styles, IDs, classes/attributes/pseudo-classes, and elements — compared from left to right. A higher value in any column wins regardless of lower columns. Inline styles always beat external stylesheets; !important overrides all specificity but makes styles unmanageable. In practice: rely on class selectors, avoid IDs in CSS, and never fight specificity wars with !important.",
    "codeExample": "/* Specificity scores */\np              { color: black; }  /* 0,0,0,1 */\n.text          { color: blue; }   /* 0,0,1,0 — wins */\n#main .text    { color: red; }    /* 0,1,1,0 — wins over .text */",
    "relatedSlugs": [
      "css-cascade",
      "css-box-model"
    ]
  },
  {
    "slug": "css-cascade",
    "label": "Cascade",
    "topic": "css",
    "tooltip": "The algorithm that determines which CSS declaration applies when multiple rules target the same property on the same element.",
    "definition": "The cascade resolves conflicts between CSS declarations using three tiers in order: origin and importance (browser defaults < author styles < user styles; !important reverses this), then specificity (higher specificity wins), then source order (later rules win). CSS Cascade Layers (@layer) give you explicit control over origin order without fighting specificity.",
    "codeExample": "@layer base, components, utilities;\n\n@layer base {\n  a { color: blue; }\n}\n\n@layer utilities {\n  .text-red { color: red; } /* utilities layer wins */\n}",
    "relatedSlugs": [
      "css-specificity",
      "css-custom-property"
    ]
  },
  {
    "slug": "css-flexbox",
    "label": "Flexbox",
    "topic": "css",
    "tooltip": "A one-dimensional layout system for distributing space and aligning items along a row or column.",
    "definition": "Flexbox is a one-dimensional layout model — it lays items out along a single axis (row or column). The container controls direction (flex-direction), wrapping (flex-wrap), main-axis alignment (justify-content), and cross-axis alignment (align-items). Children control how much space they take up (flex-grow, flex-shrink, flex-basis). Flexbox is ideal for navigation bars, card rows, centred content, and any layout where items need to share available space along one axis.",
    "codeExample": ".nav {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  gap: 16px;\n}\n\n.nav__logo { flex: none; }\n.nav__links { flex: 1; }",
    "relatedSlugs": [
      "css-grid",
      "css-box-model"
    ]
  },
  {
    "slug": "css-grid",
    "label": "CSS Grid",
    "topic": "css",
    "tooltip": "A two-dimensional layout system for placing elements into rows and columns simultaneously.",
    "definition": "CSS Grid is a two-dimensional layout system — it controls rows and columns at the same time, unlike Flexbox which is one-dimensional. Define the grid with grid-template-columns and grid-template-rows (using fr units for flexible tracks). grid-template-areas gives a visual ASCII-art layout map. Use Grid for page layout and two-dimensional components; use Flexbox for content alignment within cells.",
    "codeExample": ".layout {\n  display: grid;\n  grid-template-columns: 240px 1fr;\n  grid-template-areas:\n    'sidebar header'\n    'sidebar main'\n    'sidebar footer';\n}\n\n.header  { grid-area: header; }\n.sidebar { grid-area: sidebar; }\n.main    { grid-area: main; }",
    "relatedSlugs": [
      "css-flexbox",
      "css-media-query"
    ]
  },
  {
    "slug": "css-custom-property",
    "label": "CSS Custom Property",
    "topic": "css",
    "tooltip": "Variables in CSS, declared with -- prefix, inherited through the DOM tree and readable in JavaScript.",
    "definition": "CSS custom properties (CSS variables) are properties whose names start with -- and whose values can be referenced with var(). They inherit through the DOM tree, so setting a variable on :root makes it available everywhere. Unlike preprocessor variables, they are live — changing them in JS or via a media query updates every dependent rule without a rebuild. They are the foundation of CSS theming.",
    "codeExample": ":root {\n  --color-primary: #6366f1;\n  --radius-md: 6px;\n}\n\n.button {\n  background: var(--color-primary);\n  border-radius: var(--radius-md);\n}\n\n/* Theme switch via JS */\ndocument.documentElement.style.setProperty('--color-primary', '#ec4899');",
    "relatedSlugs": [
      "css-cascade",
      "css-media-query"
    ]
  },
  {
    "slug": "css-pseudo-class",
    "label": "Pseudo-class",
    "topic": "css",
    "tooltip": "A keyword added to a selector that targets elements in a specific state, like :hover, :focus, or :nth-child.",
    "definition": "A pseudo-class selects elements based on their state or position rather than their attributes. Common pseudo-classes: :hover, :focus, :focus-visible (keyboard only), :disabled, :checked, :nth-child(n), :not(selector), :is(), and :has() (parent selector). Pseudo-elements (::before, ::after, ::placeholder) are distinct — they style generated content rather than states.",
    "codeExample": ".button:hover { background: var(--color-primary-dark); }\n.input:focus-visible { outline: 2px solid var(--color-primary); }\n.input:disabled { opacity: 0.5; }\n\n/* :has() — style parent based on child */\n.field:has(.input:invalid) .label { color: red; }",
    "relatedSlugs": [
      "css-specificity",
      "css-cascade"
    ]
  },
  {
    "slug": "css-media-query",
    "label": "Media Query",
    "topic": "css",
    "tooltip": "A CSS rule that applies styles conditionally based on viewport size, device capabilities, or user preferences.",
    "definition": "Media queries apply CSS rules only when specified conditions are true. Most commonly used for responsive breakpoints (@media (min-width: 768px)), but also for user preferences: prefers-color-scheme: dark, prefers-reduced-motion: reduce. Mobile-first means writing base styles for small screens and using min-width queries to progressively enhance for larger viewports. TailwindCSS's sm:, md:, lg: prefixes compile to min-width media queries.",
    "codeExample": ".grid { display: grid; grid-template-columns: 1fr; }\n\n@media (min-width: 768px) {\n  .grid { grid-template-columns: repeat(2, 1fr); }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  * { animation-duration: 0.01ms !important; }\n}",
    "relatedSlugs": [
      "css-grid",
      "css-custom-property"
    ]
  },
  {
    "slug": "css-stacking-context",
    "label": "Stacking Context",
    "topic": "css",
    "tooltip": "An isolated layer in the z-axis where z-index values are compared only within that context, not against the whole page.",
    "definition": "A stacking context is a three-dimensional space where elements with z-index compete. A new stacking context is created by any element with position + non-auto z-index, opacity < 1, transform, filter, will-change, or several other properties. Within a stacking context, z-index values are only compared against siblings — child elements can never escape their stacking context. This is why a z-index: 9999 inside a transformed element sometimes disappears behind a modal.",
    "codeExample": "/* Why z-index: 9999 can still lose */\n.parent {\n  transform: translateX(0); /* new stacking context */\n  z-index: 1;\n}\n.child {\n  z-index: 9999; /* only competes within .parent */\n}\n.modal {\n  z-index: 10; /* beats .parent (z-index 1) */\n}",
    "relatedSlugs": [
      "css-specificity",
      "css-cascade"
    ]
  },
  {
    "slug": "css-bfc",
    "label": "Block Formatting Context",
    "topic": "css",
    "tooltip": "An isolated formatting region where block-level boxes are laid out independently, containing floats and preventing margin collapse.",
    "definition": "A Block Formatting Context (BFC) is a self-contained layout region. Inside a BFC, vertical margins don't collapse with elements outside, float children are contained, and the box doesn't overlap floated siblings. BFCs are created by overflow: hidden/auto, display: flow-root, display: flex/grid, and position: absolute/fixed. display: flow-root is the modern, intention-revealing way to create a BFC.",
    "codeExample": "/* Contain floated children */\n.container {\n  display: flow-root; /* creates BFC, no clearfix hack needed */\n}\n\n.float-child {\n  float: left;\n  width: 200px;\n}",
    "relatedSlugs": [
      "css-box-model",
      "css-flexbox"
    ]
  },
  {
    "slug": "use-state",
    "label": "useState",
    "topic": "react-hooks",
    "tooltip": "The hook for adding local state to a function component — returns the current value and a setter function.",
    "definition": "useState declares a state variable and returns a pair: the current value and a setter. Calling the setter schedules a re-render with the new value. Pass a function to the setter to derive next state from previous (setCount(c => c + 1)) — always use the function form when new state depends on old state to avoid stale closure bugs. Pass a function to the initialiser (useState(() => expensiveCalc())) for lazy initialisation that runs only once.",
    "codeExample": "function Counter() {\n  const [count, setCount] = useState(0);\n  return (\n    <button onClick={() => setCount(c => c + 1)}>\n      Count: {count}\n    </button>\n  );\n}\n\n// Lazy initialiser\nconst [data, setData] = useState(() => JSON.parse(localStorage.getItem('data') ?? '[]'));",
    "relatedSlugs": [
      "use-reducer",
      "use-effect",
      "use-memo"
    ]
  },
  {
    "slug": "use-effect",
    "label": "useEffect",
    "topic": "react-hooks",
    "tooltip": "The hook for running side effects (data fetching, subscriptions, DOM mutations) after render.",
    "definition": "useEffect runs a side effect after the browser has painted. The dependency array controls when it runs: empty array [] = once on mount, specific deps = when any dep changes, no array = every render. The cleanup function (returned from the effect) runs before the next execution and on unmount. In React 18 Strict Mode, effects mount → unmount → remount in development to surface cleanup bugs. For data fetching, prefer TanStack Query or SWR over raw useEffect.",
    "codeExample": "useEffect(() => {\n  const controller = new AbortController();\n\n  fetch(`/api/user/${id}`, { signal: controller.signal })\n    .then(r => r.json())\n    .then(setUser)\n    .catch(e => { if (e.name !== 'AbortError') setError(e); });\n\n  return () => controller.abort();\n}, [id]);",
    "relatedSlugs": [
      "use-state",
      "use-ref",
      "use-callback"
    ]
  },
  {
    "slug": "use-memo",
    "label": "useMemo",
    "topic": "react-hooks",
    "tooltip": "Memoises an expensive computed value, recomputing only when its dependencies change.",
    "definition": "useMemo caches the return value of a function between renders, recomputing only when its dependencies change. Use it when the computation is genuinely expensive (filtering/sorting large lists) and is triggered on frequent re-renders. Don't reach for it by default — memoisation has a cost and most computations are fast enough. useMemo is for values; useCallback is the equivalent for functions.",
    "codeExample": "function ProductList({ products, category }) {\n  const filtered = useMemo(\n    () => products.filter(p => p.category === category),\n    [products, category],\n  );\n  return <ul>{filtered.map(p => <li key={p.id}>{p.name}</li>)}</ul>;\n}",
    "relatedSlugs": [
      "use-callback",
      "use-state",
      "use-ref"
    ]
  },
  {
    "slug": "use-callback",
    "label": "useCallback",
    "topic": "react-hooks",
    "tooltip": "Returns a memoised function reference that only changes when its dependencies change — prevents unnecessary child re-renders.",
    "definition": "useCallback returns a stable function reference that only changes when its dependencies change. Its primary purpose is to prevent child components wrapped in React.memo from re-rendering unnecessarily because a new function reference was created on every parent render. Like useMemo, reach for it only when you have measured a real performance problem.",
    "codeExample": "const handleSubmit = useCallback(async (formData) => {\n  await api.saveUser(formData);\n  navigate('/dashboard');\n}, [navigate]);\n\nconst MemoizedForm = React.memo(ExpensiveForm);\nreturn <MemoizedForm onSubmit={handleSubmit} />;",
    "relatedSlugs": [
      "use-memo",
      "use-state"
    ]
  },
  {
    "slug": "use-ref",
    "label": "useRef",
    "topic": "react-hooks",
    "tooltip": "Returns a mutable ref object whose .current property persists across renders without triggering re-renders.",
    "definition": "useRef returns an object { current: T } that persists across renders. Mutating .current does NOT trigger a re-render. Two uses: holding a reference to a DOM node (set via the ref prop) and storing mutable values that survive renders without causing them (interval IDs, previous values, abort controllers).",
    "codeExample": "// DOM ref\nfunction AutoFocusInput() {\n  const inputRef = useRef(null);\n  useEffect(() => { inputRef.current?.focus(); }, []);\n  return <input ref={inputRef} />;\n}\n\n// Mutable value — store interval ID\nconst intervalRef = useRef();\nuseEffect(() => {\n  intervalRef.current = setInterval(tick, 1000);\n  return () => clearInterval(intervalRef.current);\n}, []);",
    "relatedSlugs": [
      "use-state",
      "use-effect",
      "use-memo"
    ]
  },
  {
    "slug": "use-context",
    "label": "useContext",
    "topic": "react-hooks",
    "tooltip": "Subscribes a component to a React context, re-rendering when the context value changes.",
    "definition": "useContext reads the nearest Context.Provider value above in the tree and subscribes to it — the component re-renders whenever the context value changes. Create a context with createContext(defaultValue); wrap a subtree in Context.Provider; consume with useContext(Context). Context is React's built-in dependency injection for subtrees.",
    "codeExample": "const ThemeContext = createContext('light');\n\nfunction App() {\n  const [theme, setTheme] = useState('light');\n  return (\n    <ThemeContext.Provider value={theme}>\n      <Header />\n    </ThemeContext.Provider>\n  );\n}\n\nfunction Header() {\n  const theme = useContext(ThemeContext);\n  return <nav className={theme}>...</nav>;\n}",
    "relatedSlugs": [
      "use-state",
      "use-reducer"
    ]
  },
  {
    "slug": "use-reducer",
    "label": "useReducer",
    "topic": "react-hooks",
    "tooltip": "An alternative to useState for complex state logic — manages state with a (state, action) => newState reducer.",
    "definition": "useReducer is an alternative to useState for state with complex transitions, multiple sub-values, or inter-dependent fields. It takes a reducer (state, action) => newState and initial state, returning current state and a dispatch function. Combine with useContext to distribute state through a subtree — the lightweight alternative to Redux for feature-level state.",
    "codeExample": "function reducer(state, action) {\n  switch (action.type) {\n    case 'increment': return { ...state, count: state.count + state.step };\n    case 'setStep':   return { ...state, step: action.payload };\n  }\n}\n\nconst [state, dispatch] = useReducer(reducer, { count: 0, step: 1 });\ndispatch({ type: 'increment' });",
    "relatedSlugs": [
      "use-state",
      "use-context",
      "redux-reducer"
    ]
  },
  {
    "slug": "use-id",
    "label": "useId",
    "topic": "react-hooks",
    "tooltip": "Generates a stable, unique ID for linking form labels to inputs — safe for server-side rendering.",
    "definition": "useId generates a unique, stable ID string consistent between server and client renders, making it safe for SSR/hydration. Its primary use is linking label htmlFor to input id inside reusable components — without a unique ID, multiple instances would share the same id and break accessibility. Do not use useId to generate list keys.",
    "codeExample": "function TextField({ label }) {\n  const id = useId();\n  return (\n    <div>\n      <label htmlFor={id}>{label}</label>\n      <input id={id} />\n    </div>\n  );\n}\n\n// Multiple instances get different IDs\n<TextField label=\"Email\" />    // id=\":r0:\"\n<TextField label=\"Password\" /> // id=\":r1:\"",
    "relatedSlugs": [
      "use-ref",
      "use-state"
    ]
  },
  {
    "slug": "use-transition",
    "label": "useTransition",
    "topic": "react-hooks",
    "tooltip": "Marks a state update as non-urgent so React can keep the UI responsive while the update is pending.",
    "definition": "useTransition returns [isPending, startTransition]. Wrapping a state update in startTransition tells React it is non-urgent — the browser can interrupt it to handle more urgent updates like typing. isPending is true while the transition is in-flight. Use it for expensive renders triggered by navigation or filter changes to keep the input field responsive.",
    "codeExample": "function SearchPage() {\n  const [query, setQuery] = useState('');\n  const [results, setResults] = useState([]);\n  const [isPending, startTransition] = useTransition();\n\n  function handleChange(e) {\n    setQuery(e.target.value); // urgent\n    startTransition(() => {\n      setResults(heavyFilter(e.target.value)); // non-urgent\n    });\n  }\n\n  return (\n    <>\n      <input value={query} onChange={handleChange} />\n      {isPending ? <Spinner /> : <ResultList results={results} />}\n    </>\n  );\n}",
    "relatedSlugs": [
      "use-state",
      "use-memo"
    ]
  },
  {
    "slug": "git-commit",
    "label": "Commit",
    "topic": "git",
    "tooltip": "A snapshot of the repository's state at a point in time, identified by a SHA hash.",
    "definition": "A Git commit is an immutable snapshot of the entire repository at a point in time. It contains the file tree, a pointer to the parent commit(s), metadata (author, timestamp), and a message. Good commits are atomic (one logical change), have a descriptive message in imperative mood ('Add validation', not 'Added validation'), and pass tests.",
    "codeExample": "git add src/components/Button.tsx\ngit commit -m 'Add loading state to Button component'\n\n# Amend the last commit (don't do on shared branches)\ngit commit --amend -m 'Add loading state and test to Button'\n\ngit log --oneline --graph",
    "relatedSlugs": [
      "git-branch",
      "git-rebase"
    ]
  },
  {
    "slug": "git-branch",
    "label": "Branch",
    "topic": "git",
    "tooltip": "A lightweight movable pointer to a commit — the standard way to isolate work in progress from the main codebase.",
    "definition": "A Git branch is simply a movable pointer to a commit. Creating a branch is cheap — it only creates a 41-byte file. HEAD points to the currently checked-out branch, and each new commit advances that branch pointer forward. Feature branches isolate work so multiple developers can work in parallel without interfering.",
    "codeExample": "git switch -c feature/user-auth\ngit push -u origin feature/user-auth\n\n# Delete after merging\ngit branch -d feature/user-auth\ngit push origin --delete feature/user-auth",
    "relatedSlugs": [
      "git-commit",
      "git-merge",
      "git-rebase"
    ]
  },
  {
    "slug": "git-rebase",
    "label": "Rebase",
    "topic": "git",
    "tooltip": "Replays commits from one branch onto another, producing a linear history without merge commits.",
    "definition": "Rebase rewrites commit history by replaying your branch's commits on top of another branch. The result is a linear history — no merge commits. Interactive rebase (git rebase -i) lets you squash, reorder, edit, or drop commits before pushing. Golden rule: never rebase commits already pushed to a shared branch — it rewrites history.",
    "codeExample": "git switch feature/my-feature\ngit fetch origin\ngit rebase origin/main\n\n# Interactive rebase — squash last 3 commits\ngit rebase -i HEAD~3\n\n# Resolve conflicts\ngit add <resolved-files>\ngit rebase --continue",
    "relatedSlugs": [
      "git-merge",
      "git-commit",
      "git-branch"
    ]
  },
  {
    "slug": "git-merge",
    "label": "Merge",
    "topic": "git",
    "tooltip": "Combines two branches by creating a merge commit that has both branches as parents.",
    "definition": "Merge integrates changes from one branch into another. A fast-forward merge simply moves the pointer forward when there are no diverging commits. A three-way merge creates a new merge commit with two parents when histories have diverged. Merge preserves the full branch history. --no-ff forces a merge commit even when fast-forward is possible.",
    "codeExample": "git switch main\ngit merge feature/user-auth\n\n# Force a merge commit\ngit merge --no-ff feature/user-auth -m 'Merge feature/user-auth'\n\n# Abort a conflicted merge\ngit merge --abort",
    "relatedSlugs": [
      "git-rebase",
      "git-branch",
      "git-commit"
    ]
  },
  {
    "slug": "git-stash",
    "label": "Stash",
    "topic": "git",
    "tooltip": "Temporarily saves uncommitted changes so you can switch context and come back later.",
    "definition": "Git stash saves your uncommitted changes to a stack and restores a clean working tree. Useful when you need to switch branches without committing half-finished work. git stash pop applies and removes the latest stash; git stash apply applies without removing. Don't use stash as long-term storage — commit on a WIP branch instead.",
    "codeExample": "git stash push -m 'WIP: form validation'\ngit stash list\n\n# Apply and remove\ngit stash pop\n\n# Apply to a different branch\ngit stash apply stash@{0}",
    "relatedSlugs": [
      "git-commit",
      "git-branch"
    ]
  },
  {
    "slug": "git-cherry-pick",
    "label": "Cherry-pick",
    "topic": "git",
    "tooltip": "Applies a specific commit from another branch onto the current branch without merging the whole branch.",
    "definition": "Cherry-pick copies one or more commits from anywhere in history and applies them as new commits on the current branch. Common uses: backport a bug fix to a release branch without merging all of main, apply a commit from the wrong branch. The new commits have different SHAs but the same diff.",
    "codeExample": "git cherry-pick a3f8c1e\n\n# Range of commits\ngit cherry-pick a3f8c1e^..f9b2e7d\n\n# Stage only (don't commit)\ngit cherry-pick --no-commit a3f8c1e",
    "relatedSlugs": [
      "git-commit",
      "git-rebase"
    ]
  },
  {
    "slug": "git-reset",
    "label": "Reset",
    "topic": "git",
    "tooltip": "Moves HEAD to a previous commit — --soft keeps changes staged, --mixed unstages, --hard discards everything.",
    "definition": "Git reset moves the HEAD pointer to a specified commit. Three modes: --soft moves HEAD only (staged changes kept); --mixed (default) moves HEAD and clears the index (working tree unchanged); --hard moves HEAD and discards all changes. Reset on already-pushed commits is destructive — prefer git revert to create an undo commit instead.",
    "codeExample": "# Undo last commit, keep changes staged\ngit reset --soft HEAD~1\n\n# Undo last commit, unstage changes\ngit reset HEAD~1\n\n# Unstage a specific file\ngit reset HEAD src/app.ts\n\n# DANGER: discard last 2 commits entirely\ngit reset --hard HEAD~2",
    "relatedSlugs": [
      "git-commit",
      "git-rebase"
    ]
  },
  {
    "slug": "pull-request",
    "label": "Pull Request",
    "topic": "git",
    "tooltip": "A GitHub/GitLab mechanism to propose, review, discuss, and merge changes from a branch into another.",
    "definition": "A Pull Request (PR) — Merge Request in GitLab — is a structured workflow for proposing code changes. The author pushes a branch, opens a PR against a target branch, and reviewers leave inline comments, approve, or request changes. PRs enable async code review, enforce CI checks, and document why a change was made. Good PR hygiene: small focused scope, descriptive title, self-review before opening.",
    "codeExample": "# 1. Create and push feature branch\ngit switch -c feature/add-search\ngit push -u origin feature/add-search\n\n# 2. Open PR on GitHub — describe what and why\n# 3. CI runs (lint, tests, build)\n# 4. Reviewer approves\n# 5. Merge (squash / merge / rebase per team convention)\n# 6. Delete the feature branch",
    "relatedSlugs": [
      "git-branch",
      "git-merge",
      "git-rebase"
    ]
  },
  {
    "slug": "sprint",
    "label": "Sprint",
    "topic": "agile-scrum",
    "tooltip": "A fixed time-box (usually 1–4 weeks) in which a Scrum team completes a set of agreed work from the backlog.",
    "definition": "A Sprint is the core time-box of Scrum — a fixed period (typically two weeks) during which the team builds a potentially shippable product increment. It begins with Sprint Planning, runs through Daily Standups, and ends with a Sprint Review (demo) and Sprint Retrospective (process improvement). Sprints have a fixed end date — unfinished work is not automatically carried over.",
    "codeExample": "Sprint 42 (2 weeks):\n├── Sprint Planning  → select backlog items\n├── Day 1–9         → build, test, review\n│   └── Daily Standup each morning\n├── Day 10 AM       → Sprint Review (demo)\n└── Day 10 PM       → Sprint Retrospective",
    "relatedSlugs": [
      "user-story",
      "velocity",
      "retrospective",
      "standup"
    ]
  },
  {
    "slug": "user-story",
    "label": "User Story",
    "topic": "agile-scrum",
    "tooltip": "A short description of a feature written from the user's perspective: As a [role], I want [goal], so that [benefit].",
    "definition": "A user story captures a software feature from the end user's perspective. Format: As a [role], I want [goal], so that [benefit]. Stories should be INVEST: Independent, Negotiable, Valuable, Estimable, Small, Testable. Acceptance criteria (Given/When/Then) define when the story is done. Epics are large stories that must be split into smaller stories before sprint planning.",
    "codeExample": "User Story:\n  As a registered user,\n  I want to reset my password via email,\n  so that I can regain access when I forget it.\n\nAcceptance Criteria:\n  Given I click 'Forgot password' and enter my email\n  When I submit\n  Then I receive a reset link within 5 minutes",
    "relatedSlugs": [
      "sprint",
      "backlog",
      "story-point"
    ]
  },
  {
    "slug": "backlog",
    "label": "Product Backlog",
    "topic": "agile-scrum",
    "tooltip": "An ordered list of everything the team might work on — features, bugs, tech debt — maintained by the Product Owner.",
    "definition": "The Product Backlog is the single, ordered list of all work the team might ever do. The Product Owner owns and prioritises it. Items at the top are refined (estimated, acceptance criteria defined) and sprint-ready; items lower down are rough and large. Backlog refinement sessions happen mid-sprint to prepare upcoming items. The backlog is never done — it evolves as the product grows.",
    "codeExample": "Product Backlog (prioritised):\n  [P0] Log in with Google             → 3 pts, refined\n  [P0] Fix: session cookie expires    → 1 pt, refined\n  [P1] Export data as CSV             → 5 pts, refined\n  [P1] Migrate to App Router          → 13 pts, rough\n  [P2] Dark mode                      → 8 pts, rough",
    "relatedSlugs": [
      "user-story",
      "sprint",
      "story-point"
    ]
  },
  {
    "slug": "retrospective",
    "label": "Retrospective",
    "topic": "agile-scrum",
    "tooltip": "A meeting at the end of each sprint where the team reflects on their process and agrees on improvements.",
    "definition": "The Sprint Retrospective is a time-boxed meeting (typically 90 minutes for a 2-week sprint) where the team inspects how they worked. Common format: Start/Stop/Continue. The output is a small number of concrete, actionable improvements. Retros work only if the team feels psychologically safe enough to raise real problems.",
    "codeExample": "Sprint 42 Retrospective:\n\nStart:\n  - Writing ADRs before big changes\n  - Pairing on complex tickets\n\nStop:\n  - Merging PRs without a second reviewer\n\nContinue:\n  - Async standups via Slack\n  - 85% test coverage threshold",
    "relatedSlugs": [
      "sprint",
      "standup"
    ]
  },
  {
    "slug": "velocity",
    "label": "Velocity",
    "topic": "agile-scrum",
    "tooltip": "The average story points a team completes per sprint — used for release forecasting, not performance measurement.",
    "definition": "Velocity is the sum of story points completed across each sprint, averaged over the last 3–5 sprints. It is used for forecasting — not for comparing teams or measuring individual performance. A sudden velocity drop signals impediments; consistent rise usually means the team is splitting stories smaller.",
    "codeExample": "Sprint 40: 18 pts\nSprint 41: 22 pts\nSprint 42: 20 pts\nSprint 43: 19 pts\n\nAverage: 79 / 4 = ~20 pts/sprint\n\nForecasting: 160 pts remaining\n→ 160 / 20 = 8 sprints ≈ 16 weeks",
    "relatedSlugs": [
      "sprint",
      "story-point",
      "backlog"
    ]
  },
  {
    "slug": "story-point",
    "label": "Story Point",
    "topic": "agile-scrum",
    "tooltip": "A relative unit of effort used to estimate user stories — measuring complexity and uncertainty, not time.",
    "definition": "Story points are a relative measure of effort — complexity, uncertainty, and volume combined — not hours. Teams use a Fibonacci-like scale (1, 2, 3, 5, 8, 13). Points are relative to each other and team-specific. Key rule: never convert story points to hours — that destroys their value as relative estimates.",
    "codeExample": "Planning Poker:\n  Story: 'Filter products by price range'\n\n  Aaron (FE):  5 → non-trivial slider + URL state\n  Sam (BE):    3 → API already handles range queries\n  Priya (QA):  5 → currency formatting edge cases\n\n  Discussion, consensus: 5 pts",
    "relatedSlugs": [
      "user-story",
      "velocity",
      "sprint"
    ]
  },
  {
    "slug": "standup",
    "label": "Daily Standup",
    "topic": "agile-scrum",
    "tooltip": "A short daily sync (15 minutes max) where the team shares progress, plans, and blockers.",
    "definition": "The Daily Scrum is a 15-minute time-boxed event covering: what did I do yesterday, what will I do today, are there blockers. The goal is coordination and early blocker detection — not status reporting. Detailed problem-solving should go offline after the standup. Async standups (Slack updates) are common for distributed teams.",
    "codeExample": "Yesterday:\n  - Shipped search filter component\n  - Reviewed Sam's API changes\n\nToday:\n  - Pick up FE-421: pagination\n  - Pair with Priya on a11y audit\n\nBlockers:\n  - Waiting on design approval for modal variant",
    "relatedSlugs": [
      "sprint",
      "retrospective"
    ]
  },
  {
    "slug": "definition-of-done",
    "label": "Definition of Done",
    "topic": "agile-scrum",
    "tooltip": "A shared checklist that defines what 'complete' means for any work item — code, tests, docs, review, deploy.",
    "definition": "The Definition of Done (DoD) is a shared agreement of what done means for every item leaving the board. It ensures consistent quality. A typical DoD: code reviewed and approved, tests passing, coverage threshold met, accessibility checked, deployed to staging. The DoD is not per-story acceptance criteria — it is the minimum quality bar for all work.",
    "codeExample": "Definition of Done:\n  ✅ PR reviewed by at least one peer\n  ✅ All CI tests pass\n  ✅ Code coverage >= 85%\n  ✅ No TypeScript strict errors\n  ✅ WCAG 2.1 AA checked\n  ✅ Deployed to staging and smoke-tested",
    "relatedSlugs": [
      "sprint",
      "user-story"
    ]
  },
  {
    "slug": "kanban",
    "label": "Kanban",
    "topic": "agile-scrum",
    "tooltip": "A lean workflow management method using a visual board of columns with WIP limits to surface bottlenecks.",
    "definition": "Kanban is a lean workflow management method using a visual board where work items flow through columns (Backlog → In Progress → Review → Done). Work-in-Progress (WIP) limits cap how many items can be in each column, surfacing bottlenecks. Unlike Scrum, Kanban has no sprints, no fixed iterations, and no prescribed roles. It suits teams with unpredictable, interrupt-driven work (support, ops, maintenance).",
    "codeExample": "Kanban Board (WIP limit = 3):\n┌──────────┬──────────────────┬────────────┬──────────┐\n│ Backlog  │  In Progress (3) │ In Review  │   Done   │\n├──────────┼──────────────────┼────────────┼──────────┤\n│ FE-445   │ FE-441 (Aaron)   │ FE-440     │ FE-438   │\n│ FE-446   │ FE-442 (Sam)     │ FE-439     │ FE-437   │\n└──────────┴──────────────────┴────────────┴──────────┘",
    "relatedSlugs": [
      "sprint",
      "backlog"
    ]
  },
  {
    "slug": "websocket",
    "label": "WebSocket",
    "topic": "websockets",
    "tooltip": "A protocol providing full-duplex communication over a single persistent TCP connection between client and server.",
    "definition": "WebSocket is a communication protocol providing full-duplex, bidirectional messaging over a persistent TCP connection. Unlike HTTP's request-response cycle, a WebSocket connection stays open — both client and server can send messages independently at any time. Ideal for: live chat, collaborative editing, live dashboards, multiplayer games. URIs use ws:// or wss:// (TLS-encrypted).",
    "codeExample": "const ws = new WebSocket('wss://api.example.com/ws');\n\nws.addEventListener('open', () => {\n  ws.send(JSON.stringify({ type: 'subscribe', channel: 'prices' }));\n});\n\nws.addEventListener('message', (event) => {\n  const data = JSON.parse(event.data);\n  updatePrices(data);\n});",
    "relatedSlugs": [
      "ws-handshake",
      "ws-heartbeat",
      "server-sent-events"
    ]
  },
  {
    "slug": "ws-handshake",
    "label": "WebSocket Handshake",
    "topic": "websockets",
    "tooltip": "The HTTP Upgrade request that establishes a WebSocket connection and switches protocols from HTTP to WS.",
    "definition": "The WebSocket handshake starts as an HTTP/1.1 request with an Upgrade: websocket header and a Sec-WebSocket-Key. The server responds with 101 Switching Protocols. From this point, the TCP connection is no longer HTTP — it is a raw WebSocket channel. The handshake traverses HTTP infrastructure, so WebSocket-aware routing is required.",
    "codeExample": "// Client → Server:\nGET /ws HTTP/1.1\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==\n\n// Server → Client:\nHTTP/1.1 101 Switching Protocols\nUpgrade: websocket\nSec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=\n\n// TCP now carries WebSocket frames",
    "relatedSlugs": [
      "websocket",
      "ws-heartbeat"
    ]
  },
  {
    "slug": "ws-heartbeat",
    "label": "Heartbeat / Ping-Pong",
    "topic": "websockets",
    "tooltip": "Periodic messages exchanged to detect dead connections — firewalls silently drop idle TCP connections.",
    "definition": "Long-lived WebSocket connections can appear open while the underlying TCP connection is silently dead — firewalls and NAT devices drop them without a close frame. Heartbeats detect this: the server sends a ping every N seconds; if no pong arrives within the timeout, the connection is considered dead and the client reconnects with exponential backoff.",
    "codeExample": "// Server-side heartbeat (ws library)\nwss.on('connection', (ws) => {\n  ws.isAlive = true;\n  ws.on('pong', () => { ws.isAlive = true; });\n\n  const timer = setInterval(() => {\n    if (!ws.isAlive) { ws.terminate(); return; }\n    ws.isAlive = false;\n    ws.ping();\n  }, 30_000);\n\n  ws.on('close', () => clearInterval(timer));\n});",
    "relatedSlugs": [
      "websocket",
      "ws-reconnect"
    ]
  },
  {
    "slug": "ws-reconnect",
    "label": "Reconnection Strategy",
    "topic": "websockets",
    "tooltip": "The pattern of automatically re-establishing a WebSocket connection after disconnection, using exponential backoff.",
    "definition": "WebSocket connections drop — network blips, server restarts, mobile app backgrounding. A good reconnection strategy retries automatically with exponential backoff: start short, increase delay geometrically, add jitter to prevent thundering herd. Cap the maximum delay (e.g. 30 seconds). On reconnection, re-subscribe to channels and request missed events.",
    "codeExample": "function connectWithBackoff(url) {\n  let attempt = 0;\n  function connect() {\n    const ws = new WebSocket(url);\n    ws.onopen = () => { attempt = 0; };\n    ws.onclose = () => {\n      const delay = Math.min(1000 * 2 ** attempt, 30_000)\n        + Math.random() * 1000;\n      attempt++;\n      setTimeout(connect, delay);\n    };\n  }\n  connect();\n}",
    "relatedSlugs": [
      "websocket",
      "ws-heartbeat"
    ]
  },
  {
    "slug": "server-sent-events",
    "label": "Server-Sent Events",
    "topic": "websockets",
    "tooltip": "A unidirectional HTTP streaming mechanism where the server pushes updates to the client over a long-lived connection.",
    "definition": "Server-Sent Events (SSE) is a W3C standard for the server to push a stream of text events to the browser over a single persistent HTTP connection. Unlike WebSocket, SSE is one-directional (server to client only) and uses plain HTTP — works through standard proxies. The browser's EventSource API handles automatic reconnection. SSE is ideal for live feeds, notifications, and streaming AI responses.",
    "codeExample": "// Next.js Route Handler\nexport async function GET() {\n  const stream = new ReadableStream({\n    start(controller) {\n      const enc = new TextEncoder();\n      let n = 0;\n      const id = setInterval(() => {\n        controller.enqueue(enc.encode(`data: ${n++}\\n\\n`));\n        if (n > 10) { clearInterval(id); controller.close(); }\n      }, 1000);\n    },\n  });\n  return new Response(stream, {\n    headers: { 'Content-Type': 'text/event-stream' },\n  });\n}\n\n// Client\nconst source = new EventSource('/api/stream');\nsource.onmessage = (e) => console.log(e.data);",
    "relatedSlugs": [
      "websocket",
      "ws-heartbeat"
    ]
  },
  {
    "slug": "socket-io",
    "label": "Socket.IO",
    "topic": "websockets",
    "tooltip": "A library wrapping WebSocket with rooms, namespaces, automatic reconnection, and HTTP long-polling fallback.",
    "definition": "Socket.IO wraps WebSocket (with HTTP long-polling fallback) and adds: rooms (broadcast to groups), namespaces (multiple channels on one connection), automatic reconnection, and event acknowledgements. Not a pure WebSocket implementation — a Socket.IO client cannot connect to a raw WebSocket server. Best for chat, collaborative apps, and dashboards where rooms and broadcast patterns simplify the code significantly.",
    "codeExample": "// Server\nconst io = new Server(httpServer);\nio.on('connection', (socket) => {\n  socket.on('join-room', (roomId) => {\n    socket.join(roomId);\n    socket.to(roomId).emit('user-joined', socket.id);\n  });\n  socket.on('message', ({ roomId, text }) => {\n    io.to(roomId).emit('message', { text, from: socket.id });\n  });\n});\n\n// Client\nconst socket = io('wss://api.example.com');\nsocket.emit('join-room', 'room-42');",
    "relatedSlugs": [
      "websocket",
      "ws-reconnect"
    ]
  },
  {
    "slug": "semantic-html",
    "label": "Semantic HTML",
    "topic": "html5",
    "tooltip": "Using HTML elements that describe their meaning — nav, main, article, section — rather than generic divs.",
    "definition": "Semantic HTML means choosing elements that describe the meaning of content, not just its appearance. HTML5 landmarks: header, nav, main, article, section, aside, footer. Benefits: screen readers can navigate by landmark, search engines understand page structure, other developers understand intent. The anti-pattern is 'div soup' — nested divs with class names doing work that HTML elements should carry.",
    "codeExample": "<body>\n  <header>\n    <nav aria-label=\"Main\">...</nav>\n  </header>\n  <main>\n    <article>\n      <h1>Understanding Closures</h1>\n    </article>\n    <aside>Related articles</aside>\n  </main>\n  <footer>...</footer>\n</body>",
    "relatedSlugs": [
      "aria",
      "service-worker"
    ]
  },
  {
    "slug": "aria",
    "label": "ARIA",
    "topic": "html5",
    "tooltip": "Accessible Rich Internet Applications — attributes that expose interactive widget roles and states to assistive technologies.",
    "definition": "ARIA (Accessible Rich Internet Applications) is a set of HTML attributes (role, aria-*) that communicate element roles, states, and properties to assistive technologies. First rule: don't use ARIA — use native HTML elements first. ARIA is for when native elements are insufficient: custom widgets (combobox, dialog), dynamic regions (aria-live), and labelling (aria-label, aria-labelledby). Incorrect ARIA is worse than no ARIA.",
    "codeExample": "<!-- Native — no ARIA needed -->\n<button type=\"submit\">Save</button>\n\n<!-- Custom dialog needs ARIA -->\n<div role=\"dialog\" aria-modal=\"true\" aria-labelledby=\"dialog-title\">\n  <h2 id=\"dialog-title\">Confirm Delete</h2>\n</div>\n\n<!-- Live region -->\n<div aria-live=\"polite\">{statusMessage}</div>",
    "relatedSlugs": [
      "semantic-html"
    ]
  },
  {
    "slug": "web-storage",
    "label": "Web Storage",
    "topic": "html5",
    "tooltip": "Browser APIs (localStorage and sessionStorage) for storing key-value string data client-side.",
    "definition": "Web Storage provides two mechanisms: localStorage (persists until explicitly cleared) and sessionStorage (cleared when the tab closes). Both store only strings — use JSON.stringify/parse for objects. Storage is synchronous and origin-scoped. Size limit is typically 5–10 MB. Note: localStorage is not available in SSR environments — guard with typeof window !== 'undefined'.",
    "codeExample": "localStorage.setItem('theme', 'dark');\nconst theme = localStorage.getItem('theme');\n\n// Objects\nlocalStorage.setItem('user', JSON.stringify({ name: 'Aaron' }));\nconst user = JSON.parse(localStorage.getItem('user') ?? 'null');\n\n// SSR guard\nconst stored = typeof window !== 'undefined'\n  ? localStorage.getItem('prefs')\n  : null;",
    "relatedSlugs": [
      "service-worker",
      "semantic-html"
    ]
  },
  {
    "slug": "service-worker",
    "label": "Service Worker",
    "topic": "html5",
    "tooltip": "A script that runs in a background thread, enabling offline caching, push notifications, and network interception.",
    "definition": "A Service Worker is a JavaScript file running in a background thread, separate from the main page. It acts as a programmable network proxy — intercepting fetch requests to serve from cache (offline support), managing push notifications, and background sync. Service workers have no DOM access and communicate via postMessage. They persist across page loads and are scoped to their location in the origin.",
    "codeExample": "// sw.js\nconst CACHE = 'v1';\nconst ASSETS = ['/', '/index.html', '/styles.css'];\n\nself.addEventListener('install', (e) => {\n  e.waitUntil(caches.open(CACHE).then(c => c.addAll(ASSETS)));\n});\n\nself.addEventListener('fetch', (e) => {\n  e.respondWith(\n    caches.match(e.request).then(cached => cached ?? fetch(e.request))\n  );\n});",
    "relatedSlugs": [
      "web-storage",
      "semantic-html"
    ]
  },
  {
    "slug": "canvas-html",
    "label": "Canvas",
    "topic": "html5",
    "tooltip": "An HTML element with a JavaScript API for drawing 2D graphics procedurally — immediate mode, no retained scene graph.",
    "definition": "The canvas element provides a pixel-based drawing surface accessed via the Canvas 2D API (getContext('2d')) or WebGL. Unlike SVG, canvas is immediate mode — you draw by calling imperative methods and the canvas has no memory of what was drawn. Fast for many items (charts, particle systems, games) but harder to make accessible (hit detection is manual). Libraries like Fabric.js, Konva, and Three.js abstract canvas for specific use cases.",
    "codeExample": "const canvas = document.getElementById('chart');\nconst ctx = canvas.getContext('2d');\n\nctx.fillStyle = '#6366f1';\nctx.fillRect(50, 50, 100, 200);\n\nctx.fillStyle = '#111';\nctx.font = '16px sans-serif';\nctx.fillText('Revenue', 75, 40);\n\nfunction render() {\n  ctx.clearRect(0, 0, canvas.width, canvas.height);\n  requestAnimationFrame(render);\n}\nrender();",
    "relatedSlugs": [
      "semantic-html",
      "web-storage"
    ]
  }
]