[
  {
    "topic": "react-hooks",
    "question": "What are React Hooks?",
    "options": [
      {
        "optionText": "Functions starting with 'use' that let function components access React features like state, effects, refs, and context without class components.",
        "isCorrect": true,
        "explanation": "Correct — hooks were introduced in React 16.8 and follow two rules: call at top level, call only from React functions."
      },
      {
        "optionText": "Lifecycle methods that replace componentDidMount and componentDidUpdate in class components.",
        "isCorrect": false,
        "explanation": "Hooks are for function components — they replace class components, not just lifecycle methods."
      },
      {
        "optionText": "Third-party plugins that extend React with additional functionality.",
        "isCorrect": false,
        "explanation": "Hooks are a core React feature, not third-party plugins."
      },
      {
        "optionText": "Special HTML attributes that hook into browser events like onclick and onchange.",
        "isCorrect": false,
        "explanation": "HTML event attributes are unrelated to React Hooks — hooks are JavaScript functions that tap into React's internal state system."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "What is useState() in React?",
    "options": [
      {
        "optionText": "A hook that declares a state variable and returns the current value plus a setter function. Calling the setter schedules a re-render.",
        "isCorrect": true,
        "explanation": "Correct — always use the function form of the setter (setCount(c => c + 1)) when new state depends on previous state."
      },
      {
        "optionText": "A hook that stores state in the browser's localStorage for persistence across page refreshes.",
        "isCorrect": false,
        "explanation": "useState keeps state in React's memory — for persistence, you need to sync to localStorage separately."
      },
      {
        "optionText": "A hook that provides access to global Redux state without a store setup.",
        "isCorrect": false,
        "explanation": "useState manages local component state — for global state use Redux, Zustand, or Context."
      },
      {
        "optionText": "A hook that re-renders the component on every state change and all parent state changes.",
        "isCorrect": false,
        "explanation": "useState only causes the component to re-render when its own state changes, not parent state (unless props change)."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "How to call a loading function with React useEffect only once?",
    "options": [
      {
        "optionText": "Pass an empty dependency array [] as the second argument — the effect runs once on mount and the cleanup runs on unmount.",
        "isCorrect": true,
        "explanation": "Correct — [] tells React the effect has no dependencies and should not re-run after the initial render."
      },
      {
        "optionText": "Call useEffect without a second argument — effects without a dependency array run only once.",
        "isCorrect": false,
        "explanation": "Without a dependency array, useEffect runs after every render — you must pass [] to run it once."
      },
      {
        "optionText": "Wrap the function in useCallback with an empty array and call it inside useEffect.",
        "isCorrect": false,
        "explanation": "useCallback memoises a function reference — the key to running an effect once is the [] dependency array."
      },
      {
        "optionText": "Use useState to track whether the function has been called, and skip on subsequent renders.",
        "isCorrect": false,
        "explanation": "That is a workaround — passing [] to useEffect is the correct, built-in solution."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "How to access DOM elements in React?",
    "options": [
      {
        "optionText": "Create a ref with useRef(null), attach it to the element via the ref prop, then access the DOM node via ref.current.",
        "isCorrect": true,
        "explanation": "Correct — React sets ref.current to the DOM node after mount and null on unmount."
      },
      {
        "optionText": "Use document.getElementById() inside a useEffect — this is the recommended approach.",
        "isCorrect": false,
        "explanation": "Direct DOM queries bypass React's rendering model — useRef is the idiomatic React way to access DOM nodes."
      },
      {
        "optionText": "Access DOM elements via the event.target property from any event handler.",
        "isCorrect": false,
        "explanation": "event.target works for the specific element that triggered the event, but useRef is for direct DOM access without waiting for an event."
      },
      {
        "optionText": "Use useState to store a reference to the DOM node after the component first renders.",
        "isCorrect": false,
        "explanation": "useState triggers re-renders when updated — useRef stores mutable values without causing re-renders."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "What are the advantages of using React Hooks?",
    "options": [
      {
        "optionText": "Simpler code (no class boilerplate), easier logic reuse via custom hooks, co-location of related logic, and better TypeScript inference.",
        "isCorrect": true,
        "explanation": "Correct — custom hooks allow sharing stateful logic without HOCs or render props, which were the pre-hooks solutions."
      },
      {
        "optionText": "Hooks make React components render faster by bypassing the virtual DOM.",
        "isCorrect": false,
        "explanation": "Hooks do not bypass the virtual DOM — they work within React's existing rendering model."
      },
      {
        "optionText": "Hooks allow React components to run on the server without any modifications.",
        "isCorrect": false,
        "explanation": "Server-side rendering is handled separately — hooks are a component authoring API, not an SSR optimisation."
      },
      {
        "optionText": "Hooks automatically memoize all computed values to prevent unnecessary re-renders.",
        "isCorrect": false,
        "explanation": "Memoization requires explicit use of useMemo and useCallback — hooks do not memoize automatically."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "When writing a Custom Hook, what is the difference between it and a normal function?",
    "options": [
      {
        "optionText": "A custom hook must start with 'use', can call other hooks internally, and follows hook rules. A normal function cannot call hooks.",
        "isCorrect": true,
        "explanation": "Correct — the 'use' prefix signals to React (and linters) that hook rules apply — calls other hooks at the top level."
      },
      {
        "optionText": "Custom hooks are defined outside the component; normal functions are defined inside.",
        "isCorrect": false,
        "explanation": "Both can be defined outside components — the distinction is the 'use' prefix and the ability to call other hooks."
      },
      {
        "optionText": "Custom hooks automatically re-render the component when called; normal functions do not.",
        "isCorrect": false,
        "explanation": "A custom hook re-renders the component only if it contains hooks like useState or useContext that trigger renders."
      },
      {
        "optionText": "Custom hooks require a return statement; normal functions do not.",
        "isCorrect": false,
        "explanation": "Both can return anything or nothing — the distinction is hook rule compliance, not syntax."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "Do two components using the same Hook share state?",
    "options": [
      {
        "optionText": "No — each component that calls a hook gets its own independent state. Hooks don't create shared singletons.",
        "isCorrect": true,
        "explanation": "Correct — to share state between components, lift it up or use Context/external stores like Zustand."
      },
      {
        "optionText": "Yes — state is shared between all components that use the same custom hook.",
        "isCorrect": false,
        "explanation": "Each hook call creates isolated state — hooks are a per-instance mechanism."
      },
      {
        "optionText": "It depends on whether the hook is exported as a singleton from a module.",
        "isCorrect": false,
        "explanation": "How a hook is exported is irrelevant — each invocation of a hook creates independent state."
      },
      {
        "optionText": "State is shared only if both components are in the same component tree.",
        "isCorrect": false,
        "explanation": "Being in the same tree does not cause hook state to be shared — each component call creates its own state."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "When and how would you use useContext?",
    "options": [
      {
        "optionText": "When a value (theme, auth, locale) needs to be available to many components at different nesting levels without prop drilling. Create context, wrap with Provider, consume with useContext.",
        "isCorrect": true,
        "explanation": "Correct — useContext is React's dependency injection — avoid using it for frequently-changing values as it re-renders all consumers."
      },
      {
        "optionText": "Whenever you need to share state between sibling components that have no common parent.",
        "isCorrect": false,
        "explanation": "Sibling components can share state via a common ancestor — context is for deep trees, not specifically siblings."
      },
      {
        "optionText": "useContext replaces Redux for all state management needs.",
        "isCorrect": false,
        "explanation": "Context re-renders all consumers on any value change — for complex/frequent state updates, Redux or Zustand scale better."
      },
      {
        "optionText": "useContext allows you to access browser APIs like localStorage and navigator.",
        "isCorrect": false,
        "explanation": "Browser APIs are accessed directly — context is for sharing React state through the component tree."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "When to use useState vs useReducer?",
    "options": [
      {
        "optionText": "useState for simple independent values; useReducer for complex state with multiple sub-values, inter-dependent transitions, or when the next state depends on the action type.",
        "isCorrect": true,
        "explanation": "Correct — useReducer also centralises update logic, making each transition independently testable."
      },
      {
        "optionText": "useReducer requires Redux — useState is the built-in React alternative.",
        "isCorrect": false,
        "explanation": "useReducer is built into React and does not require Redux — it is a simpler, local version of the Redux pattern."
      },
      {
        "optionText": "useState is for primitive values; useReducer is for objects and arrays.",
        "isCorrect": false,
        "explanation": "useState handles objects and arrays fine — the distinction is complexity and transition logic, not data type."
      },
      {
        "optionText": "useReducer is deprecated in React 18 — always use useState with the functional updater pattern.",
        "isCorrect": false,
        "explanation": "useReducer is not deprecated — it is actively recommended for complex state logic."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "What are the use cases for useRef?",
    "options": [
      {
        "optionText": "Storing DOM node references for direct manipulation (focus, measure), and storing mutable values that survive renders without triggering re-renders (interval IDs, previous values).",
        "isCorrect": true,
        "explanation": "Correct — unlike useState, mutating ref.current does not schedule a re-render."
      },
      {
        "optionText": "useRef is used exclusively for accessing DOM elements — state values should use useState.",
        "isCorrect": false,
        "explanation": "useRef is also used for any mutable value that should persist across renders without causing re-renders."
      },
      {
        "optionText": "useRef persists data to localStorage so it survives page refreshes.",
        "isCorrect": false,
        "explanation": "useRef stores values in React's memory for the component's lifetime — it does not persist beyond the component unmounting."
      },
      {
        "optionText": "useRef memoises expensive calculations between renders, like useMemo.",
        "isCorrect": false,
        "explanation": "useRef stores a mutable value — useMemo caches computed values and re-runs when dependencies change."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "Explain the difference between useState and useRef hooks.",
    "options": [
      {
        "optionText": "useState triggers a re-render when updated; useRef does not. Both persist values across renders, but only useState drives UI updates.",
        "isCorrect": true,
        "explanation": "Correct — this is why useRef is ideal for storing timer IDs or previous values that shouldn't cause renders."
      },
      {
        "optionText": "useState is for strings and numbers; useRef is for objects and DOM elements.",
        "isCorrect": false,
        "explanation": "Both handle any value type — the distinction is re-render behaviour, not data type."
      },
      {
        "optionText": "useRef is a newer hook that replaces useState in React 18.",
        "isCorrect": false,
        "explanation": "Both coexist in React 18 with distinct purposes — useRef does not replace useState."
      },
      {
        "optionText": "useState values are synchronous; useRef values are updated asynchronously.",
        "isCorrect": false,
        "explanation": "Both are synchronous in terms of mutation — state updates are batched for rendering, not asynchronous updates."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "When and why would you use useMemo()?",
    "options": [
      {
        "optionText": "When a computation is genuinely expensive (filtering thousands of items, complex maths) and runs on frequent re-renders — memoize to avoid redundant work.",
        "isCorrect": true,
        "explanation": "Correct — don't reach for useMemo by default; profile first. Most computations are fast enough without it."
      },
      {
        "optionText": "Always wrap every computed value in useMemo to make the component as fast as possible.",
        "isCorrect": false,
        "explanation": "useMemo has overhead (memory, comparison cost) — over-using it can make performance worse."
      },
      {
        "optionText": "useMemo is for memoising function references to prevent child re-renders — that's its primary use.",
        "isCorrect": false,
        "explanation": "Memoising functions is useCallback — useMemo is for memoising computed values."
      },
      {
        "optionText": "useMemo prevents re-renders caused by parent component state changes.",
        "isCorrect": false,
        "explanation": "useMemo memoises a value but doesn't prevent re-renders — wrap the component in React.memo to skip renders."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "What are common use cases for useCallback?",
    "options": [
      {
        "optionText": "Stabilising function references passed to React.memo-wrapped children, and dependencies in useEffect or other hooks where a new function reference would trigger unnecessary re-runs.",
        "isCorrect": true,
        "explanation": "Correct — without useCallback, a new function is created every render, potentially causing memoized children to re-render."
      },
      {
        "optionText": "useCallback is used to cache the result of asynchronous API calls between renders.",
        "isCorrect": false,
        "explanation": "Caching API results is handled by TanStack Query or useMemo — useCallback memoises function references."
      },
      {
        "optionText": "useCallback prevents the function from being called more than once during a render cycle.",
        "isCorrect": false,
        "explanation": "useCallback does not limit how many times a function is called — it stabilises the reference."
      },
      {
        "optionText": "useCallback is used to debounce event handlers in React components.",
        "isCorrect": false,
        "explanation": "Debouncing requires a timer (usually in a useRef or useMemo) — useCallback just memoises the function reference."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "What are the differences between React.memo() and useMemo()?",
    "options": [
      {
        "optionText": "React.memo wraps a component to skip re-rendering if props haven't changed; useMemo caches a computed value inside a component.",
        "isCorrect": true,
        "explanation": "Correct — React.memo is a component-level optimisation; useMemo is a value-level optimisation within a component."
      },
      {
        "optionText": "React.memo is for class components; useMemo is for function components.",
        "isCorrect": false,
        "explanation": "Both apply to function components — React.memo wraps the component; useMemo is a hook inside it."
      },
      {
        "optionText": "They are identical — React.memo is the JSX syntax for useMemo.",
        "isCorrect": false,
        "explanation": "They are completely different tools — React.memo prevents component re-renders; useMemo caches computed values."
      },
      {
        "optionText": "useMemo is deprecated in React 18 in favour of React.memo.",
        "isCorrect": false,
        "explanation": "Both are actively used in React 18 — they solve different problems and are not interchangeable."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "Do Hooks replace render props and higher-order components (HOC)?",
    "options": [
      {
        "optionText": "Largely yes — custom hooks share stateful logic more cleanly without wrapper nesting. But HOCs still have valid uses (e.g. third-party library integrations, code splitting boundaries).",
        "isCorrect": true,
        "explanation": "Correct — hooks reduce 'wrapper hell' from deeply nested HOCs and render props, but don't completely obsolete HOCs."
      },
      {
        "optionText": "No — hooks, HOCs, and render props solve completely different problems and cannot replace each other.",
        "isCorrect": false,
        "explanation": "Hooks do overlap significantly with HOCs and render props for stateful logic sharing — custom hooks are now the preferred approach."
      },
      {
        "optionText": "Yes completely — HOCs and render props are deprecated as of React 16.8.",
        "isCorrect": false,
        "explanation": "HOCs and render props are not deprecated — they remain valid patterns, though hooks are preferred for logic sharing."
      },
      {
        "optionText": "Only render props are replaced by hooks — HOCs are still the recommended pattern for cross-cutting concerns.",
        "isCorrect": false,
        "explanation": "Hooks largely replace both render props and HOCs for logic sharing, though HOCs retain some valid use cases."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "Do React Hooks cover all use cases for class components?",
    "options": [
      {
        "optionText": "Almost — the only missing hook is getSnapshotBeforeUpdate and (until React 19) error boundaries. All other class lifecycle functionality has hook equivalents.",
        "isCorrect": true,
        "explanation": "Correct — error boundaries still require class components (or third-party wrappers like react-error-boundary) as of React 18."
      },
      {
        "optionText": "Yes completely — there is no reason to ever use a class component with hooks available.",
        "isCorrect": false,
        "explanation": "Error boundaries are a notable exception — they still require class components in React 18."
      },
      {
        "optionText": "No — class components are fundamentally more capable and hooks are a simplified subset.",
        "isCorrect": false,
        "explanation": "Hooks cover virtually all class component use cases — the React team recommends hooks for new code."
      },
      {
        "optionText": "Hooks only cover useState and lifecycle methods — complex patterns still require class components.",
        "isCorrect": false,
        "explanation": "Hooks cover state, effects, context, refs, reducers, memoization, and more — far beyond just state and lifecycle."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "Which lifecycle methods of class components are replaced by useEffect in functional components?",
    "options": [
      {
        "optionText": "componentDidMount (empty deps), componentDidUpdate (specific deps), and componentWillUnmount (cleanup function).",
        "isCorrect": true,
        "explanation": "Correct — useEffect unifies three lifecycle methods. The cleanup function returned by useEffect runs on unmount and before each re-run."
      },
      {
        "optionText": "Only componentDidMount — useEffect cannot replicate update or unmount behaviour.",
        "isCorrect": false,
        "explanation": "useEffect handles all three: mount (empty deps []), update (specific deps), and unmount (return cleanup). It's a unified API."
      },
      {
        "optionText": "shouldComponentUpdate — useEffect controls when a component re-renders.",
        "isCorrect": false,
        "explanation": "shouldComponentUpdate controls re-render optimisation, which is handled by React.memo and useMemo — not useEffect."
      },
      {
        "optionText": "componentWillMount and componentWillReceiveProps — the deprecated lifecycle methods.",
        "isCorrect": false,
        "explanation": "Those deprecated methods are not replaced by useEffect. useEffect handles mount, update, and unmount — not the legacy willMount/willReceiveProps patterns."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "What is batching in React and what are its advantages?",
    "options": [
      {
        "optionText": "React groups multiple state updates from the same event into one re-render — React 18 extends this to async and setTimeout too.",
        "isCorrect": true,
        "explanation": "Correct — before React 18, batching only happened in event handlers. React 18's automatic batching covers promises, setTimeout, and native events, reducing renders."
      },
      {
        "optionText": "Batching splits a large component into smaller chunks to improve code-splitting performance.",
        "isCorrect": false,
        "explanation": "That describes code splitting with React.lazy/Suspense. Batching is about grouping multiple state updates into a single render."
      },
      {
        "optionText": "Batching means React pre-fetches data for the next render before the current one completes.",
        "isCorrect": false,
        "explanation": "Pre-fetching is unrelated to batching. Batching is the consolidation of multiple setState calls into one re-render cycle."
      },
      {
        "optionText": "Batching only works with class components — functional components with hooks render immediately on each setState.",
        "isCorrect": false,
        "explanation": "Batching works with hooks. React 18 made automatic batching universal — it applies to useState, useReducer, and any state update."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "Does React useState Hook update immediately?",
    "options": [
      {
        "optionText": "No — state updates are asynchronous. The new value is not available until the next render.",
        "isCorrect": true,
        "explanation": "Correct — calling setState schedules a re-render. Reading state immediately after calling the setter still returns the old value. Use the functional update form for derived state."
      },
      {
        "optionText": "Yes — useState updates synchronously, so you can read the new value immediately after calling the setter.",
        "isCorrect": false,
        "explanation": "State updates are not synchronous. The setter schedules a re-render — the new value is only available after React re-renders the component."
      },
      {
        "optionText": "It depends — useState is synchronous in event handlers but asynchronous in useEffect.",
        "isCorrect": false,
        "explanation": "useState is always asynchronous regardless of where the setter is called. The update is batched and applied on the next render."
      },
      {
        "optionText": "useState updates immediately only when used with the functional update form: setState(prev => prev + 1).",
        "isCorrect": false,
        "explanation": "The functional form ensures you use the latest state as input, but the update itself is still asynchronous — not immediately available."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "How do I update state on a nested object with useState()?",
    "options": [
      {
        "optionText": "Spread the previous state and override only the changed fields: setState(prev => ({ ...prev, address: { ...prev.address, city: 'London' } }))",
        "isCorrect": true,
        "explanation": "Correct — useState does not deep-merge like class component setState. You must spread all levels to avoid overwriting sibling fields."
      },
      {
        "optionText": "Call setState multiple times, once for each field — React will merge them automatically.",
        "isCorrect": false,
        "explanation": "useState replaces state entirely on each call — it doesn't merge. Multiple calls will batch but each replaces the whole state object."
      },
      {
        "optionText": "Use Object.assign(prevState, { city: 'London' }) — this mutates the object in place safely.",
        "isCorrect": false,
        "explanation": "Mutating state directly is forbidden in React. Always create a new object reference; React uses reference equality to detect changes."
      },
      {
        "optionText": "Use useReducer instead — useState cannot handle nested objects.",
        "isCorrect": false,
        "explanation": "useState can handle nested objects — you just need to spread correctly. useReducer helps with complex update logic but isn't required for nested state."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "Are there any problems when using useCallback?",
    "options": [
      {
        "optionText": "Yes — useCallback adds memoisation overhead and only helps when the child is wrapped in React.memo; overusing it can hurt performance.",
        "isCorrect": true,
        "explanation": "Correct — useCallback is a premature optimisation if the child re-renders anyway. Profile first; add useCallback only when measurable benefit exists."
      },
      {
        "optionText": "No — useCallback always improves performance by preventing unnecessary function recreation.",
        "isCorrect": false,
        "explanation": "useCallback itself has a cost: comparing dependencies on every render. If the wrapped function is cheap, useCallback adds overhead without benefit."
      },
      {
        "optionText": "useCallback can cause memory leaks by holding stale references to DOM nodes.",
        "isCorrect": false,
        "explanation": "Memory leaks from stale references are a useEffect concern (forgotten cleanup). useCallback memoises functions but doesn't retain DOM nodes."
      },
      {
        "optionText": "useCallback breaks if the dependency array is empty — it must always list its dependencies.",
        "isCorrect": false,
        "explanation": "An empty dependency array is valid — it memoises the function for the component's lifetime. The linter may warn but it doesn't break."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "When would you want to avoid useEffect and use useLayoutEffect instead?",
    "options": [
      {
        "optionText": "When you need to read or write DOM measurements before the browser paints — to avoid visible layout flicker.",
        "isCorrect": true,
        "explanation": "Correct — useLayoutEffect fires synchronously after DOM mutations but before paint. Use it for tooltips, scroll restoration, or measuring element sizes."
      },
      {
        "optionText": "When fetching data — useLayoutEffect is faster than useEffect for API calls.",
        "isCorrect": false,
        "explanation": "useLayoutEffect blocks painting and should never be used for data fetching. Use useEffect or server-side data loading for async operations."
      },
      {
        "optionText": "When you want effects to run only once — useLayoutEffect ignores the dependency array.",
        "isCorrect": false,
        "explanation": "useLayoutEffect uses the same dependency array API as useEffect. Pass [] for mount-only, specific deps for updates."
      },
      {
        "optionText": "Always — useLayoutEffect is a strict replacement for useEffect with better timing.",
        "isCorrect": false,
        "explanation": "useLayoutEffect blocks the browser paint and can hurt performance. Use it only when visual consistency requires synchronous DOM reads before paint."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "When would you use flushSync in React?",
    "options": [
      {
        "optionText": "When you need DOM updates to apply synchronously before continuing — e.g. scrolling to a newly added item immediately after adding it.",
        "isCorrect": true,
        "explanation": "Correct — flushSync opts out of batching for a specific update. Common use cases: third-party DOM libraries, scroll-to-new-item, or animations that depend on immediate state."
      },
      {
        "optionText": "To flush stale cache entries from React Query or SWR.",
        "isCorrect": false,
        "explanation": "flushSync is a React DOM API for synchronous rendering — it has nothing to do with data cache invalidation in React Query or SWR."
      },
      {
        "optionText": "flushSync should replace useEffect cleanup to synchronously cancel async operations.",
        "isCorrect": false,
        "explanation": "Cancelling async operations uses AbortController inside useEffect cleanup. flushSync forces synchronous DOM updates, not async cancellation."
      },
      {
        "optionText": "flushSync is required when updating state in setTimeout — automatic batching doesn't work there.",
        "isCorrect": false,
        "explanation": "React 18 automatic batching covers setTimeout. flushSync is for the rare case you need synchronous DOM mutation, not to work around batching."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "How can I force a component to re-render with Hooks in React?",
    "options": [
      {
        "optionText": "Use a dummy state counter: const [, forceRender] = useReducer(n => n + 1, 0) — call forceRender() to trigger a re-render.",
        "isCorrect": true,
        "explanation": "Correct — this is the idiomatic hooks pattern for forced re-renders. That said, needing a forced re-render usually indicates a state design problem worth revisiting."
      },
      {
        "optionText": "Call this.forceUpdate() — it works in functional components with hooks.",
        "isCorrect": false,
        "explanation": "this.forceUpdate() is a class component API. Functional components have no this — use a state update to trigger a re-render."
      },
      {
        "optionText": "Set a ref to a new value — changing a ref always triggers a re-render.",
        "isCorrect": false,
        "explanation": "Refs do not trigger re-renders. useRef mutations are invisible to React's rendering system — only state and prop changes cause re-renders."
      },
      {
        "optionText": "Use ReactDOM.render() to re-mount the component from the outside.",
        "isCorrect": false,
        "explanation": "Re-mounting from outside destroys and recreates state. The idiomatic solution is an internal state update, not external re-mounting."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "What's the difference between useCallback and useMemo in practice?",
    "options": [
      {
        "optionText": "useCallback memoises a function reference; useMemo memoises a computed value. useCallback(fn, deps) === useMemo(() => fn, deps).",
        "isCorrect": true,
        "explanation": "Correct — use useCallback when passing a stable callback to a memoised child; use useMemo when an expensive calculation should only recompute on specific dep changes."
      },
      {
        "optionText": "useMemo is for functions; useCallback is for values — they are the reverse of each other.",
        "isCorrect": false,
        "explanation": "It's the opposite: useCallback memoises a function; useMemo memoises a value (the result of calling a function)."
      },
      {
        "optionText": "useCallback works only with event handlers; useMemo works with any computation.",
        "isCorrect": false,
        "explanation": "useCallback can memoize any function, not just event handlers. The difference is what they return: a function vs a computed value."
      },
      {
        "optionText": "They are interchangeable — pick whichever reads more clearly in context.",
        "isCorrect": false,
        "explanation": "While useCallback(fn, deps) is equivalent to useMemo(() => fn, deps), they signal different intent. Use the one that matches what you're memoising — function or value."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "When to use useCallback, useMemo, and useEffect?",
    "options": [
      {
        "optionText": "useEffect: side effects & external sync. useMemo: expensive computations. useCallback: stable function references for memoised children.",
        "isCorrect": true,
        "explanation": "Correct — each has a clear domain. Mixing them (e.g. using useEffect for computation) leads to bugs. Start without memoisation and add useMemo/useCallback only when profiling shows benefit."
      },
      {
        "optionText": "All three are interchangeable — use whichever makes the code most readable.",
        "isCorrect": false,
        "explanation": "They serve distinct purposes. useEffect manages side effects; useMemo and useCallback are optimisation tools. Using them interchangeably causes subtle bugs."
      },
      {
        "optionText": "useCallback and useMemo should always be applied to every function and value for best performance.",
        "isCorrect": false,
        "explanation": "Over-memoising adds overhead from dependency comparisons. Apply useMemo/useCallback only where profiling shows a measurable win."
      },
      {
        "optionText": "useEffect replaces useMemo — compute values inside useEffect and store them in state.",
        "isCorrect": false,
        "explanation": "Storing computed values in state via useEffect causes an extra render cycle. useMemo computes synchronously during render with no extra re-render."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "Is there any problem when using useContext Hook?",
    "options": [
      {
        "optionText": "Yes — every consumer re-renders on any context value change, even if it uses only part of the value. Split contexts or memoize to fix.",
        "isCorrect": true,
        "explanation": "Correct — this is the main performance pitfall. Solutions: split into multiple contexts, use memo on consumers, or use a state manager like Zustand that supports fine-grained subscriptions."
      },
      {
        "optionText": "No — useContext is optimised internally and only re-renders components that use changed fields.",
        "isCorrect": false,
        "explanation": "React does not track which fields a consumer reads. Any change to the context value triggers all consumers — field-level granularity must be implemented manually."
      },
      {
        "optionText": "useContext causes infinite loops when used inside useEffect.",
        "isCorrect": false,
        "explanation": "useContext does not inherently cause infinite loops. Adding a context value to useEffect deps is fine as long as the context value is stable (memoised)."
      },
      {
        "optionText": "useContext cannot be used in Server Components — it only works in Client Components.",
        "isCorrect": false,
        "explanation": "While React context requires Client Components in Next.js App Router, this is a framework constraint — not an inherent problem with useContext itself."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "How can I make use of Error Boundaries in functional React components?",
    "options": [
      {
        "optionText": "Use a class component as the boundary (or a library like react-error-boundary), then wrap functional components with it.",
        "isCorrect": true,
        "explanation": "Correct — there are no error boundary hooks yet. react-error-boundary provides useErrorBoundary() and <ErrorBoundary> wrapper for functional components. In Next.js, error.tsx acts as a boundary."
      },
      {
        "optionText": "Use try/catch inside the component body — this is the functional equivalent of Error Boundaries.",
        "isCorrect": false,
        "explanation": "try/catch cannot catch errors in JSX rendering or child components. Error Boundaries use componentDidCatch which has no hook equivalent."
      },
      {
        "optionText": "Wrap the component in useErrorBoundary() from React core — it ships with React 18.",
        "isCorrect": false,
        "explanation": "useErrorBoundary is from the react-error-boundary library, not React core. React itself still requires class components for error boundary logic."
      },
      {
        "optionText": "Use the onError prop available on every React component to catch child errors.",
        "isCorrect": false,
        "explanation": "There is no onError prop on React components. Error boundaries are implemented via class lifecycle methods or the react-error-boundary library."
      }
    ]
  },
  {
    "topic": "react-hooks",
    "question": "Can a custom React hook return JSX?",
    "options": [
      {
        "optionText": "Technically yes, but it's an anti-pattern — returning JSX from a hook breaks composability and the separation between logic and rendering.",
        "isCorrect": true,
        "explanation": "Correct — if you need to return UI, create a component. Hooks should return data, state, and callbacks — not markup. Returning JSX from hooks confuses React's reconciler."
      },
      {
        "optionText": "No — hooks can only return primitive values, arrays, or objects.",
        "isCorrect": false,
        "explanation": "Hooks can return anything technically. The restriction is a convention, not a runtime rule — returning JSX works but violates the hook/component separation."
      },
      {
        "optionText": "Yes, and it's encouraged — this is the render prop pattern modernised for hooks.",
        "isCorrect": false,
        "explanation": "Render props return JSX from a prop function, not a hook. Custom hooks should handle logic; components should handle rendering."
      },
      {
        "optionText": "Only if the hook name starts with useJSX — React validates hook names to enforce this.",
        "isCorrect": false,
        "explanation": "React only enforces that hook names start with 'use'. There is no useJSX convention — the restriction on returning JSX is a best practice, not enforced by React."
      }
    ]
  }
]